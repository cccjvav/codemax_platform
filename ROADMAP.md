# 毕设服务平台 (codemax.top) — 开发路线图

> 双平台架构：**免费工具平台（引流）** + **商业平台（变现）**，通过统一认证（SSO）打通用户体系，
> 覆盖支付闭环、云存储安全下载、AI（LLM）内容解析与智能客服等核心能力。

## 技术栈基线（仓库现状）

| 层 | 选型 |
| --- | --- |
| 后端框架 | FastAPI 0.141.1 + Uvicorn |
| ORM / 数据库 | SQLAlchemy 2.0 + asyncpg（异步） / PostgreSQL |
| 认证 | python-jose (JWT) + passlib[bcrypt] |
| 其他 | pydantic 2.x + pydantic-settings，`python-multipart`（OAuth2 表单） |
| 前端 | Jinja2 SSR 出 HTML 外壳与 TDK；图表仍客户端渲染（D3.js / Mermaid / Drawio）。**不引入 Node 运行时或 Nuxt/Next 构建链** |
| 文档导出 | `python-docx`（原路线图写的 Apache POI 是 Java 库，本项目是 Python） |
| 爬虫 | `httpx` + `BeautifulSoup4`（原路线图写的 HttpClient/Jsoup 是 Java 栈） |

## 难点 → 技术方案速查

| 编号 | 难点 | 技术方案 |
| --- | --- | --- |
| 难点1 | 下载链接安全防护 | 阿里云 OSS / 腾讯云 COS；策略模式封装多云；预签名 URL；一次性下载双重校验 |
| 难点2 | 支付闭环 | 微信支付 NATIVE 扫码；订单状态机（待支付→已支付→已下载）；回调验签 + AES-GCM 解密 + 幂等处理 |
| 难点3 | 平台内容冷启动 | httpx + BeautifulSoup4；动态页面**已选 Playwright**（TD-03/191，可选依赖）；LLM 智能解析 DOM 结构并自动提取入库 |
| 难点4 | 智能客服 | 三层架构：BM25+余弦相似度 FAQ（<80ms）→ **规则意图路由**（原计划的 BERT **未做**，缺标注数据，见 TD-150 与本文 S4-02-2）→ 闲聊 LLM / 专业问题 RAG；低置信度转人工 |
| 难点5 | 在线工具矩阵 | SQL DDL 解析 + D3.js（ER 图）；LLM→Mermaid；Drawio iframe 嵌入；python-docx 导出 Word |
| 难点6 | 跨域单点登录 | OAuth2 授权码模式统一认证中心 + JWT 无状态 Token |

---

## 任务清单

### 阶段一：基础设施与双平台用户体系打通（项目基石）

> 目标：建立“免费工具平台”与“商业平台”的底层架构，实现用户统一管理。

- [x] **S1-01 项目初始化与架构搭建**
  - [x] S1-01-1 搭建多模块工程结构（工具平台模块、商业平台模块、公共授权模块等）
  - [x] S1-01-2 数据库选型（PostgreSQL）与基础表结构设计（用户表、订单表、系统配置等）
- [x] **S1-02 实现跨域单点登录 SSO（对应难点 6）**
  - [x] S1-02-1 基于 `OAuth2` 授权码模式搭建统一认证中心
  - [x] S1-02-2 实现 `JWT` 无状态 Token 的生成、解析与校验逻辑
  - [x] S1-02-3 在工具平台和商业平台接入 SSO，实现“一次注册，全平台可用”

### 阶段二：免费工具平台开发（流量获取引擎）

> 目标：开发引流工具矩阵，并通过 SEO 优化获取自然搜索流量。

- [x] **S2-01 在线工具矩阵开发（对应难点 5）**
  - [x] S2-01-1 工具1：引入 `SQL DDL` 解析库，结合 `D3.js` 开发 ER 图在线渲染功能
    - [x] 后端：`POST /tools/er-diagram`（`app/tools/sql_ddl.py`，纯标准库解析，兼容 MySQL/PostgreSQL）
    - [x] 前端：D3.js 渲染 ER 图页面（当时是 `app/static/er.html`，**后改为 SSR 页面 `/tools/er`**，`er.html` 已删除，见 TD-94；`er.js` 后迁至 `app/frontend/`，见 TD-222）
  - [x] S2-01-2 工具2：接入 LLM 接口，设计 Prompt 实现自然语言/代码到 `Mermaid` 类图的生成
    - [x] 后端：`POST /tools/mermaid`（`app/tools/llm.py`，OpenAI 兼容客户端**可注入**，测试不出网）
    - [x] 前端：Mermaid v11 渲染 + 源码可复制（当时是 `app/static/mermaid.html`，**后改为 SSR 页面 `/tools/mermaid`**，该文件已删除，见 TD-94）
  - [x] S2-01-3 工具3：使用 `iframe` 嵌入 `Drawio`，提供流程图编辑功能，并实现与本地/云端的保存交互
    - [x] 后端：`/diagrams` CRUD（新表 `sys_diagram`，**需鉴权**且只能读写自己的；`app/models.py` 与 `database init/full_init.sql` 已同步）
    - [x] 前端：`app/templates/drawio.html`（embed.diagrams.net iframe + postMessage 协议；云端保存 / 下载 .drawio / 导入本地文件）
  - [x] S2-01-4 工具4：集成 `python-docx`，实现前端图表/内容一键导出为 Word 文档的功能
    - [x] 后端：`POST /tools/word-export`（`app/tools/word.py`，DDL → 数据字典 .docx）
    - [x] 前端：ER 图页面「导出 Word」按钮（现位于 SSR 页面 `/tools/er`）
- [x] **S2-02 SEO 与流量优化**
  - [x] S2-02-1 对工具平台进行 SEO 优化（TDK 设置、SSR 服务端渲染优化、站点地图生成）
    - [x] Jinja2 SSR：`app/templates/`（base/index/er/mermaid）+ `app/routers/site.py`，不引入 Node 构建链
    - [x] `TOOLS` 清单（`app/site.py`）同时驱动路由、首页导航、sitemap 与 TDK
    - [x] `GET /sitemap.xml` + `GET /robots.txt`（`SITE_BASE_URL` 可配）
  - [x] S2-02-2 设计“免费引流 → 商业变现”的引导转化路径及 UI 提示
    - [x] `GET /shop` 商城落地页（`app/templates/shop.html`）—— **全站第一个能真正下单的页面**：
          此前 `POST /shop/orders` 是裸接口、前端零调用者，用户只能翻 `/docs` 手敲
    - [x] 全站统一登录/注册浮层（`app/frontend/auth.js` + `base.html`），顶栏与页脚全站可见
          （此前只有 drawio 页有常驻登录框，er/mermaid 页**连登录入口都没有**；
          `POST /auth/register` 也一直没有对应 UI）
    - [x] `GET /shop/orders/{order_no}` 只读状态查询，供下单页每 3 秒轮询
    - [x] 微信 `code_url` → 内联 SVG 二维码（`segno`，纯 Python 0.07 MB）
    - [ ] 埋点统计（**有意暂缓**：撞 CSP `connect-src 'self'`，且无真实流量时读不出结论，见 TD-203）

### 阶段三：商业平台核心交易闭环（变现核心）

> 目标：实现商品展示、支付购买到安全下载的完整闭环。

- [ ] **S3-01 支付闭环从零集成（对应难点 2）**
  - [x] S3-01-1 接入 **微信支付 NATIVE 扫码支付** API
    - [x] 请求签名（SHA256withRSA）+ `Authorization` 头，严格按官方五行签名串
    - [x] `POST /shop/orders`：建订单 → NATIVE 下单 → 返回 `code_url` 供前端画二维码
    - [x] 商户订单号规则：`CM` + 14 位时间 + 12 位随机 = 28 字符（列上限 32）
    - [x] 下单前判断是否有未完成订单，有则复用同一单，不重复下单
    - [ ] 真机联调（需商户号 / API 证书 / 公网回调，见 TD-113）
    - [x] 无商户号时的演示通道：`SHOP_PAY_MODE=mock` + 本站模拟收银台（复用同一套状态机，TD-124/125）
  - [x] S3-01-2 设计并实现订单状态机：`待支付` → `已支付` → `已下载`
    - [x] `app/order_state.py`：迁移边集中定义一处；重复通知幂等；迁移用原子 CAS
    - [x] HTTP 端点：`pending → paid` 由支付回调触发（S3-01-3）；`paid → downloaded` 随 S3-02-4 下载接口接
  - [x] S3-01-3 编写微信支付回调处理接口：
    - [x] S3-01-3-1 实现回调签名验证（平台证书 / 微信支付公钥两种 PEM 都支持）
    - [x] S3-01-3-2 实现 `AES-GCM` 报文解密（冻结测试向量 + 篡改必须失败）
    - [x] S3-01-3-3 **关键：** 实现接口幂等性处理（防止微信重复通知导致重复发货/计费）
    - [x] `POST /shop/pay/notify`：验签 → 解密 → 校验金额 → 迁移状态；应答体按微信规定
    - [ ] 真机联调（同 S3-01-1，见 TD-113）
- [ ] **S3-02 下载链接安全防护（对应难点 1）**
  - [ ] S3-02-1 申请并配置阿里云 OSS 和腾讯云 COS（**需密钥**；无密钥时写出的适配器无法验证签名，故 TD-128 暂不实现）
  - [x] S3-02-2 采用**策略模式 (Strategy Pattern)** 封装云存储服务，实现多云存储的平滑兼容与切换
    - [x] `app/storage.py`：`Storage` 协议 + `build_storage()` 工厂 + `LocalStorage` 实现，换后端不动业务代码
  - [x] S3-02-3 实现基于云存储的 `预签名 URL (Presigned URL)` 生成逻辑，设置合理的 URL 过期时间
    - [x] `HMAC-SHA256(SECRET_KEY, "key\nexpires")`，TTL 由 `DOWNLOAD_URL_TTL` 配（默认 300 秒）
    - [x] 签名绑定 key：拿 A 文件的签名换 key 下 B 文件必须 403（有变异验证）
  - [x] S3-02-4 开发**一次性下载双重机制**：预签名 URL 校验 + 数据库下载状态（是否已下载过）双重校验，防止资源被无限倒卖
    - [x] 第一重：`paid → downloaded` 走状态机，同一订单第二次领链接 403
    - [x] 第二重：`/shop/dl` 校验签名与过期时间，篡改 / 过期 / 换 key / 目录穿越全部拒
    - [x] `POST /shop/download/{order_no}`：未支付 403、非本人 404、文件缺失 404 且不标记已下载
          （**是 POST 不是 GET**：它会把 `paid` 烧成 `downloaded`，带副作用的接口不该用 GET，见 TD-177）

### 阶段四：AI 赋能与平台运营效率提升

> 目标：利用爬虫和 LLM 解决内容冷启动问题，并打造智能客服降低人工成本。

- [x] **S4-01 平台内容冷启动系统（对应难点 3）**
  - [x] S4-01-1 编写底层爬虫模块（封装 `httpx` + `BeautifulSoup4`；动态页面当时约定推迟，**后已选型 Playwright**，见 TD-03/191）
    - [x] `app/tools/crawler.py`：抓取（超时 / 大小上限 / 跟随重定向 / 自定义 UA）
    - [x] **SSRF 防护**：只允许 http/https，解析出的每个地址都必须是公网地址
    - [x] `to_skeleton()`：剥噪声 + 压成 DOM 骨架再喂 LLM，不把整页 HTML 丢给模型
    - [x] 动态页面抓取：**选型 Playwright**（TD-03 已定，实现见 TD-191）—— `app/tools/browser.py`，
          请求体加 `"dynamic": true` 即用无头浏览器渲染后再解析；
          SSRF / robots / 限速都在启动浏览器**之前**跑完，与静态路径共用同一套 `parse_page`
  - [x] S4-01-2 接入 LLM 接口，构建“智能解析方案”（`app/tools/extract.py`，复用 S2-01-2 的可注入 LLM 客户端）
  - [x] S4-01-3 编写并优化 LLM Prompt，使其能够自动识别抓取到的 HTML DOM 结构
    - [x] 输入是 `to_skeleton()` 压缩后的 DOM 骨架，不是整页 HTML
    - [x] 输出严格 JSON（四个固定键），能吃 ```` ```json ```` 围栏与未闭合围栏；非 JSON / 未知字段一律报清楚的错
  - [x] S4-01-4 实现由 LLM 自动提取目标内容（如博客文章、项目介绍）并入库，自适应目标网站的前端结构变更
    - [x] **LLM 只指认 CSS 选择器，正文由 BeautifulSoup 提取**（TD-135）：入库的是页面真实字节，
          不是模型改写的文本；页面改版后重新指认即可，代码不用改
    - [x] `sys_article` 表入库，`url` 唯一去重（重复抓同一篇是更新而不是新增）
    - [x] HTTP 端点等管理员角色（TD-138）：`sys_user.role` + `require_admin` 依赖
          + `POST /admin/articles/ingest`；提权只能由运维改库（迁移脚本刻意不提权）
- [ ] **S4-02 智能客服三层架构（对应难点 4）**
  - [x] S4-02-1 **第一层（高频FAQ秒回）：** 构建基于 `BM25` + `余弦相似度` 融合召回的 FAQ 检索模块，要求响应延迟 < 80ms
  - [ ] S4-02-2 **第二层（意图路由）：** 训练或微调一个 `BERT` 多分类意图路由模型，将用户输入分为：高频FAQ(60%)、通用闲聊(20%)、专业问题(20%)
    - ⚠ **仍不勾选**：BERT 模型本身没有训练 —— 训练需要三类各上千条**标注数据**，本项目一条都没有，写一个跑不起来的「模型」只会是摆设。
    - 已落地的是**接口 + 三级级联**（`app/tools/intent.py` / `app/tools/support.py`）：
      - **第一级 规则路由**（同步、零成本、确定性）：FAQ 绝对置信度 ≥ 0.40 → FAQ；命中技术词 → 专业问题；否则闲聊。每个判定都带 `reason`。
      - **第二级 语义复检**（S4-02-5）：规则置信度 < 0.35 时，用向量余弦在 FAQ 上再搜一次。专治词袋的死穴 ——「贵不贵」和「怎么收费」一个字都不重合，词袋永远匹配不上。**不需要标注数据**：FAQ 表本身就是语料，12 条启动时向量化一次。
      - **第三级 LLM 二意见**（S4-02-5）：语义也没命中才问大模型分类（`faq/chitchat/professional/unknown`），拿不准就返回 unknown 转人工，**绝不猜**。
      - 三步都不行 → 转人工（与改动前一致）。
    - **顺带解决了「没有标注数据」这个根问题**：每次级联判定都往 `codemax.intent.labels` 日志记一条 `(原文, 规则判定, 最终标签)` JSON。跑一段时间就是**真实用户分布**的标注集，那时再训 BERT 是拿真数据训 —— 比拿 BQ / LCQMC 那种「句子对相似度」语料硬套三分类靠谱（那些语料的任务定义就不是意图分类）。
    - 将来训练出模型只需加一个 `BertIntentRouter` 实现同一协议（`classify()` 刻意保持同步，正是为了这个），业务代码一行不改。**代价见 TD-150，取舍见 TD-206。**
  - [x] S4-02-3 **第三层（分支处理）：**（`app/tools/support.py` + `POST /support/ask`）
    - [x] S4-02-3-1 通用闲聊：接入轻量级 LLM 模型进行应答
    - [x] S4-02-3-2 专业问题：搭建基于 `RAG`（检索增强生成）技术的混合检索专业解答模块
      - 检索语料 = S4-01 抓取入库的 `sys_article`；复用 FAQ 的 BM25+余弦索引，取 top-3 原文片段进 prompt，并把文章标题作为 `references` 回给前端供用户核对。**不允许模型凭记忆生产事实**（TD-152）。
  - [x] S4-02-4 **兜底机制：** 配置低置信度阈值，当模型回答置信度低或识别特定场景时，触发自动转人工机制
    - 三条兜底路径都有测试：置信度 < 0.35、命中转人工关键词（人工/投诉/举报…）、LLM 上游报错。**上游 502 时返回 200 + `escalated: true`，不把异常抛给用户。**

### 阶段五：测试、优化与上线（收尾）

- [x] **S5-01 全链路测试**（`tests/test_e2e.py`，18 条）
  - [x] S5-01-1 支付流程全覆盖测试（正常支付、超时未支付、重复回调模拟等）
    - 为做「超时未支付」**补实现了 `closed` 状态**（TD-100 当初就把它标为 S5-01-1 的触发条件）：`ORDER_EXPIRE_MINUTES` 默认 30，超时单在下次下单时被关掉并另起新单，同时打通 TD-109 的死路。
    - 顺带修掉一个真 bug：`mark_paid` 只认 `pending`，导致「关单后用户扫旧二维码付了钱」会被拒 —— 收钱不发货。现在保留 `closed → paid` 这条边（TD-156）。
    - 重复回调：同一单并发确认 5 次，状态只迁移一次且**每次都返回成功**（幂等契约，微信会重试）。
  - [x] S5-01-2 多文件下载及防盗链有效性测试
    - **抓出并修复一个安全 bug**：下载端点丢弃了 `mark_downloaded()` 的返回值，并发 4 个请求**全部**拿到有效下载链接，「一次性下载」在真并发下形同虚设。变异验证（去掉该判断）会让 `test_concurrent_download_only_one_wins` 失败（TD-158）。
    - 商品改为**真的多文件 zip** 参与测试（旧测试用假 zip 头只能验字节透传），下载后能解开、条目数与逐字节内容都对得上。
  - [x] S5-01-3 SSO 单点登录跨域测试
    - 完整链路：主站登录 → 授权 `tools` 平台 → 302 回调拿 code（`state` 原样回传）→ 换 token → 访问 `/tools/ping`。
    - 明确记录**为什么不配 CORS**（TD-157）：全程 302 跳转 + 服务端换 token，无浏览器跨域 XHR。
    - 跨域安全底线：`redirect_uri` 换域名即拒；A 平台的 code 不能被 B 平台换走；跳转 URL 里不得出现 `access_token`。
- [x] **S5-02 性能与压力测试**（`tests/test_perf.py`，8 条）
  - [x] S5-02-1 智能客服模块并发响应时间测试（重点测试 FAQ 模块的 <80ms 指标）
    - 检索本体实测均值 **0.049 ms**、p99 0.089 ms，余量约三个数量级；20 路并发打接口 p95 ≈ 15 ms。**瓶颈不在算法**，在 HTTP/鉴权/DB 会话开销。
    - 断言一律用**相对量**（比值、增长阶）而非绝对毫秒数：CI 机器快慢差好几倍，写死阈值只会变成随机红的噪音。
  - [x] S5-02-2 工具平台渲染大文件（如复杂 SQL 转 ER 图）的性能优化
    - `parse_ddl` 实测是干净的**线性**（规模 ×4 耗时 ×2.01/2.01/2.00/2.05/1.98），算法本身没问题。
    - 真正的问题是**它跑在事件循环里**：满额 20000 字符 DDL 单次 33 ms，5 个并发时最后一个等 169 ms（≈5×33，完全串行），期间全站请求排队。已把 `parse_ddl` 与 `build_data_dictionary` 移入线程池（TD-159）。
    - 效果实测：1 个大 DDL + 20 个客服请求，客服 p50 从 **45.9 ms → 14.5 ms**；5 个大 DDL + 20 个客服，p50 从 **180.0 ms → 16.5 ms**，超 80ms 的请求从 **20/20 → 5/20**。
    - ⚠️ **残留 5/20 未消除**：线程池能让出事件循环，但 **GIL 仍使 CPU 代码串行**，这是 CPython 的固有限制（TD-160）。要彻底解决需上进程池或给重端点限并发。
- [ ] **S5-03 部署与上线**（可代码化的部分已做完，见 `docs/DEPLOY.md`；剩下的都需要外部资源）
  - [ ] S5-03-1 生产环境多云存储（OSS/COS）密钥配置 —— **需密钥**，无密钥时适配器写出来也无法验证签名（TD-128）。当前 `local` 后端单机可用，**多实例部署会出问题**。
  - [x] S5-03-2 域名解析、SSL 证书配置（**代码侧**）：TLS 在反向代理终止，应用靠 `X-Forwarded-Proto` 感知 https 并下发 HSTS —— 且只在 `TRUST_PROXY_HEADERS=true` 时才信这个头，否则伪造一个头就能把站点锁死一年。证书签发本身需在云控制台/certbot 操作，文档已写明。
  - [x] S5-03-3 系统上线与监控报警配置（**代码侧**）：`/healthz` 存活探针（**刻意不查库**，否则库一抖就会把健康实例全重启，把故障放大成雪崩）+ `/readyz` 就绪探针（真查库，失败返回 503 而非 500）；安全响应头 + CSP；结构化请求日志与 `X-Request-ID`；`ENV=production` 启动自检；Dockerfile + docker-compose。闭合 TD-90/91。

---

## 任务总览（便于导入 Jira / Notion / Trello）

| 任务编号 | 所属阶段 | 任务名称 | 优先级 | 依赖 |
| --- | --- | --- | --- | --- |
| S1-01 | 阶段一 | 项目初始化与架构搭建 | P0 | — |
| S1-02 | 阶段一 | 跨域单点登录 SSO | P0 | S1-01 |
| S2-01 | 阶段二 | 在线工具矩阵开发 | P1 | S1-01 |
| S2-02 | 阶段二 | SEO 与流量优化 | P1 | S2-01 |
| S3-01 | 阶段三 | 支付闭环从零集成 | P0 | S1-01 |
| S3-02 | 阶段三 | 下载链接安全防护 | P0 | S3-01 |
| S4-01 | 阶段四 | 平台内容冷启动系统 | P1 | S1-01 |
| S4-02 | 阶段四 | 智能客服三层架构 | P1 | S1-01 |
| S5-01 | 阶段五 | 全链路测试 | P1 | S3-01, S3-02, S1-02 |
| S5-02 | 阶段五 | 性能与压力测试 | P2 | S2-01, S4-02 |
| S5-03 | 阶段五 | 部署与上线 | P1 | S5-01, S5-02 |

---

## 💡 开发顺序建议

1. **先做基础和商业闭环（阶段一、阶段三）**：先把用户能登录、能付钱、能安全下载文件的核心骨架搭起来。
2. **再做引流工具（阶段二）**：完善各种前台小工具，这部分容易出效果，前端交互比较多。
3. **最后攻克 AI 与自动化（阶段四）**：爬虫解析和三层客服架构是技术难点（亮点），放在最后集中精力攻克，可以不断调优 LLM 的 Prompt 和 RAG 的准确率。


## 合并审查修复（2026-09-11）

- [x] **R-01 确定性缺陷与交叉验证第一批**：支付／关单原子性、配置诊断／凭证编码、HTTP解码、restore ETag、Drawio初始认证、warning、收款码说明、文档登记双向校验。SQLite 673+4，PostgreSQL 675+2。
- [ ] **R-02 后续功能与文档修复**：详见 `docs/REVIEW_CROSSCHECK.md`，不得把 R-01 完成等同于全部审查项关闭。
- [ ] **R-03 需决策的安全／权益／部署方案**：schema 与会话升级、可恢复下载、可信入口与爬虫出口、API文档及客服承诺。
