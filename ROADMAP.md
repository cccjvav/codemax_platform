# 毕设服务平台 (codemax.top) — 开发路线图

> 双平台架构：**免费工具平台（引流）** + **商业平台（变现）**，通过统一认证（SSO）打通用户体系，
> 覆盖支付闭环、云存储安全下载、AI（LLM）内容解析与智能客服等核心能力。

## 技术栈基线（仓库现状）

| 层 | 选型 |
| --- | --- |
| 后端框架 | FastAPI 0.104 + Uvicorn |
| ORM / 数据库 | SQLAlchemy 2.0 + asyncpg（异步） / PostgreSQL |
| 认证 | python-jose (JWT) + passlib[bcrypt] |
| 其他 | pydantic 2.x + pydantic-settings，`python-multipart`（OAuth2 表单） |
| 前端 | Jinja2 SSR 出 HTML 外壳与 TDK；图表仍客户端渲染（D3.js / Mermaid / Drawio）。**不引入 Node 运行时或 Nuxt/Next 构建链** |
| 文档导出 | `python-docx`（原路线图写的 Apache POI 是 Java 库，本项目是 Python） |
| 爬虫 | `httpx` + `BeautifulSoup4`（原路线图写的 HttpClient/Jsoup 是 Java 栈） |

## 难点 → 技术方案速查

| 编号 | 难点 | 技术方案 |
| --- | --- | --- |
| 难点1 | 下载链接安全防护 | 阿里云 OSS / 腾讯云 COS；策略模式封装多云；预签名 URL；一次性下载双重校验 |
| 难点2 | 支付闭环 | 微信支付 NATIVE 扫码；订单状态机（待支付→已支付→已下载）；回调验签 + AES-GCM 解密 + 幂等处理 |
| 难点3 | 平台内容冷启动 | httpx + BeautifulSoup4（动态页面阶段四再定 Selenium / Playwright）；LLM 智能解析 DOM 结构并自动提取入库 |
| 难点4 | 智能客服 | 三层架构：BM25+余弦相似度 FAQ（<80ms）→ BERT 意图路由 → 闲聊 LLM / 专业问题 RAG；低置信度转人工 |
| 难点5 | 在线工具矩阵 | SQL DDL 解析 + D3.js（ER 图）；LLM→Mermaid；Drawio iframe 嵌入；python-docx 导出 Word |
| 难点6 | 跨域单点登录 | OAuth2 授权码模式统一认证中心 + JWT 无状态 Token |

---

## 任务清单

### 阶段一：基础设施与双平台用户体系打通（项目基石）

> 目标：建立“免费工具平台”与“商业平台”的底层架构，实现用户统一管理。

- [x] **S1-01 项目初始化与架构搭建**
  - [x] S1-01-1 搭建多模块工程结构（工具平台模块、商业平台模块、公共授权模块等）
  - [x] S1-01-2 数据库选型（PostgreSQL）与基础表结构设计（用户表、订单表、系统配置等）
- [x] **S1-02 实现跨域单点登录 SSO（对应难点 6）**
  - [x] S1-02-1 基于 `OAuth2` 授权码模式搭建统一认证中心
  - [x] S1-02-2 实现 `JWT` 无状态 Token 的生成、解析与校验逻辑
  - [x] S1-02-3 在工具平台和商业平台接入 SSO，实现“一次注册，全平台可用”

### 阶段二：免费工具平台开发（流量获取引擎）

> 目标：开发引流工具矩阵，并通过 SEO 优化获取自然搜索流量。

- [x] **S2-01 在线工具矩阵开发（对应难点 5）**
  - [x] S2-01-1 工具1：引入 `SQL DDL` 解析库，结合 `D3.js` 开发 ER 图在线渲染功能
    - [x] 后端：`POST /tools/er-diagram`（`app/tools/sql_ddl.py`，纯标准库解析，兼容 MySQL/PostgreSQL）
    - [x] 前端：D3.js 渲染 ER 图页面（`app/static/er.html` + `er.js`，访问 `/static/er.html`）
  - [x] S2-01-2 工具2：接入 LLM 接口，设计 Prompt 实现自然语言/代码到 `Mermaid` 类图的生成
    - [x] 后端：`POST /tools/mermaid`（`app/tools/llm.py`，OpenAI 兼容客户端**可注入**，测试不出网）
    - [x] 前端：`app/static/mermaid.html`（Mermaid v11 渲染 + 源码可复制）
  - [x] S2-01-3 工具3：使用 `iframe` 嵌入 `Drawio`，提供流程图编辑功能，并实现与本地/云端的保存交互
    - [x] 后端：`/diagrams` CRUD（新表 `sys_diagram`，**需鉴权**且只能读写自己的；`app/models.py` 与 `database init/full_init.sql` 已同步）
    - [x] 前端：`app/templates/drawio.html`（embed.diagrams.net iframe + postMessage 协议；云端保存 / 下载 .drawio / 导入本地文件）
  - [x] S2-01-4 工具4：集成 `python-docx`，实现前端图表/内容一键导出为 Word 文档的功能
    - [x] 后端：`POST /tools/word-export`（`app/tools/word.py`，DDL → 数据字典 .docx）
    - [x] 前端：ER 图页面「导出 Word」按钮（`/static/er.html`）
- [ ] **S2-02 SEO 与流量优化**
  - [x] S2-02-1 对工具平台进行 SEO 优化（TDK 设置、SSR 服务端渲染优化、站点地图生成）
    - [x] Jinja2 SSR：`app/templates/`（base/index/er/mermaid）+ `app/routers/site.py`，不引入 Node 构建链
    - [x] `TOOLS` 清单（`app/site.py`）同时驱动路由、首页导航、sitemap 与 TDK
    - [x] `GET /sitemap.xml` + `GET /robots.txt`（`SITE_BASE_URL` 可配）
  - [ ] S2-02-2 设计“免费引流 → 商业变现”的引导转化路径及 UI 提示（本轮按选型决定跳过）

### 阶段三：商业平台核心交易闭环（变现核心）

> 目标：实现商品展示、支付购买到安全下载的完整闭环。

- [ ] **S3-01 支付闭环从零集成（对应难点 2）**
  - [x] S3-01-1 接入 **微信支付 NATIVE 扫码支付** API
    - [x] 请求签名（SHA256withRSA）+ `Authorization` 头，严格按官方五行签名串
    - [x] `POST /shop/orders`：建订单 → NATIVE 下单 → 返回 `code_url` 供前端画二维码
    - [x] 商户订单号规则：`CM` + 14 位时间 + 12 位随机 = 28 字符（列上限 32）
    - [x] 下单前判断是否有未完成订单，有则复用同一单，不重复下单
    - [ ] 真机联调（需商户号 / API 证书 / 公网回调，见 TD-113）
    - [x] 无商户号时的演示通道：`SHOP_PAY_MODE=mock` + 本站模拟收银台（复用同一套状态机，TD-124/125）
  - [x] S3-01-2 设计并实现订单状态机：`待支付` → `已支付` → `已下载`
    - [x] `app/order_state.py`：迁移边集中定义一处；重复通知幂等；迁移用原子 CAS
    - [x] HTTP 端点：`pending → paid` 由支付回调触发（S3-01-3）；`paid → downloaded` 随 S3-02-4 下载接口接
  - [x] S3-01-3 编写微信支付回调处理接口：
    - [x] S3-01-3-1 实现回调签名验证（平台证书 / 微信支付公钥两种 PEM 都支持）
    - [x] S3-01-3-2 实现 `AES-GCM` 报文解密（冻结测试向量 + 篡改必须失败）
    - [x] S3-01-3-3 **关键：** 实现接口幂等性处理（防止微信重复通知导致重复发货/计费）
    - [x] `POST /shop/pay/notify`：验签 → 解密 → 校验金额 → 迁移状态；应答体按微信规定
    - [ ] 真机联调（同 S3-01-1，见 TD-113）
- [ ] **S3-02 下载链接安全防护（对应难点 1）**
  - [ ] S3-02-1 申请并配置阿里云 OSS 和腾讯云 COS（**需密钥**；无密钥时写出的适配器无法验证签名，故 TD-128 暂不实现）
  - [x] S3-02-2 采用**策略模式 (Strategy Pattern)** 封装云存储服务，实现多云存储的平滑兼容与切换
    - [x] `app/storage.py`：`Storage` 协议 + `build_storage()` 工厂 + `LocalStorage` 实现，换后端不动业务代码
  - [x] S3-02-3 实现基于云存储的 `预签名 URL (Presigned URL)` 生成逻辑，设置合理的 URL 过期时间
    - [x] `HMAC-SHA256(SECRET_KEY, "key\nexpires")`，TTL 由 `DOWNLOAD_URL_TTL` 配（默认 300 秒）
    - [x] 签名绑定 key：拿 A 文件的签名换 key 下 B 文件必须 403（有变异验证）
  - [x] S3-02-4 开发**一次性下载双重机制**：预签名 URL 校验 + 数据库下载状态（是否已下载过）双重校验，防止资源被无限倒卖
    - [x] 第一重：`paid → downloaded` 走状态机，同一订单第二次领链接 403
    - [x] 第二重：`/shop/dl` 校验签名与过期时间，篡改 / 过期 / 换 key / 目录穿越全部拒
    - [x] `GET /shop/download/{order_no}`：未支付 403、非本人 404、文件缺失 404 且不标记已下载

### 阶段四：AI 赋能与平台运营效率提升

> 目标：利用爬虫和 LLM 解决内容冷启动问题，并打造智能客服降低人工成本。

- [ ] **S4-01 平台内容冷启动系统（对应难点 3）**
  - [x] S4-01-1 编写底层爬虫模块（封装 `httpx` + `BeautifulSoup4`，动态页面阶段四再定 `Selenium` / `Playwright`）
    - [x] `app/tools/crawler.py`：抓取（超时 / 大小上限 / 跟随重定向 / 自定义 UA）
    - [x] **SSRF 防护**：只允许 http/https，解析出的每个地址都必须是公网地址
    - [x] `to_skeleton()`：剥噪声 + 压成 DOM 骨架再喂 LLM，不把整页 HTML 丢给模型
    - [ ] 动态页面（Selenium / Playwright）仍按约定推迟
  - [ ] S4-01-2 接入 LLM 接口，构建“智能解析方案”
  - [ ] S4-01-3 编写并优化 LLM Prompt，使其能够自动识别抓取到的 HTML DOM 结构
  - [ ] S4-01-4 实现由 LLM 自动提取目标内容（如博客文章、项目介绍）并入库，自适应目标网站的前端结构变更
- [ ] **S4-02 智能客服三层架构（对应难点 4）**
  - [ ] S4-02-1 **第一层（高频FAQ秒回）：** 构建基于 `BM25` + `余弦相似度` 融合召回的 FAQ 检索模块，要求响应延迟 < 80ms
  - [ ] S4-02-2 **第二层（意图路由）：** 训练或微调一个 `BERT` 多分类意图路由模型，将用户输入分为：高频FAQ(60%)、通用闲聊(20%)、专业问题(20%)
  - [ ] S4-02-3 **第三层（分支处理）：**
    - [ ] S4-02-3-1 通用闲聊：接入轻量级 LLM 模型进行应答
    - [ ] S4-02-3-2 专业问题：搭建基于 `RAG`（检索增强生成）技术的混合检索专业解答模块
  - [ ] S4-02-4 **兜底机制：** 配置低置信度阈值，当模型回答置信度低或识别特定场景时，触发自动转人工机制

### 阶段五：测试、优化与上线（收尾）

- [ ] **S5-01 全链路测试**
  - [ ] S5-01-1 支付流程全覆盖测试（正常支付、超时未支付、重复回调模拟等）
  - [ ] S5-01-2 多文件下载及防盗链有效性测试
  - [ ] S5-01-3 SSO 单点登录跨域测试
- [ ] **S5-02 性能与压力测试**
  - [ ] S5-02-1 智能客服模块并发响应时间测试（重点测试 FAQ 模块的 <80ms 指标）
  - [ ] S5-02-2 工具平台渲染大文件（如复杂 SQL 转 ER 图）的性能优化
- [ ] **S5-03 部署与上线**
  - [ ] S5-03-1 生产环境多云存储（OSS/COS）密钥配置
  - [ ] S5-03-2 域名解析、SSL 证书配置
  - [ ] S5-03-3 系统上线与监控报警配置

---

## 任务总览（便于导入 Jira / Notion / Trello）

| 任务编号 | 所属阶段 | 任务名称 | 优先级 | 依赖 |
| --- | --- | --- | --- | --- |
| S1-01 | 阶段一 | 项目初始化与架构搭建 | P0 | — |
| S1-02 | 阶段一 | 跨域单点登录 SSO | P0 | S1-01 |
| S2-01 | 阶段二 | 在线工具矩阵开发 | P1 | S1-01 |
| S2-02 | 阶段二 | SEO 与流量优化 | P1 | S2-01 |
| S3-01 | 阶段三 | 支付闭环从零集成 | P0 | S1-01 |
| S3-02 | 阶段三 | 下载链接安全防护 | P0 | S3-01 |
| S4-01 | 阶段四 | 平台内容冷启动系统 | P1 | S1-01 |
| S4-02 | 阶段四 | 智能客服三层架构 | P1 | S1-01 |
| S5-01 | 阶段五 | 全链路测试 | P1 | S3-01, S3-02, S1-02 |
| S5-02 | 阶段五 | 性能与压力测试 | P2 | S2-01, S4-02 |
| S5-03 | 阶段五 | 部署与上线 | P1 | S5-01, S5-02 |

---

## 💡 开发顺序建议

1. **先做基础和商业闭环（阶段一、阶段三）**：先把用户能登录、能付钱、能安全下载文件的核心骨架搭起来。
2. **再做引流工具（阶段二）**：完善各种前台小工具，这部分容易出效果，前端交互比较多。
3. **最后攻克 AI 与自动化（阶段四）**：爬虫解析和三层客服架构是技术难点（亮点），放在最后集中精力攻克，可以不断调优 LLM 的 Prompt 和 RAG 的准确率。
