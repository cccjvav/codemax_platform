# 可复用经验与技能进化

## 元管理：现行 Skill 不应引用已失效的收尾环境

- 日期：2026-09-13；标签：交接、Skill、CI、文档漂移。
- 场景：本轮审阅旧 finish-subitem/pre-commit-review，发现旧分支、固定 PR/测试数量、HANDOVER 旧章节以及只看两个 CI job 的示例；其中 finish-subitem 还建议 git add -A。
- 根因：Skill 留在旧项目阶段，而当前导航和门禁已经变化；只在 AGENTS 声明优先级不足以消除旧指令被再次执行的风险。
- 处理：直接修复可访问的 Skill 源，收尾改成选择性暂存、当前固定分支、最终 SHA 全部 jobs；状态集中阶段文件，经验集中本页，不在多个入口重复固定计数。
- 预防：阶段收尾同时审阅流程本身；源与分发副本做逐字检查，提交代码块做旧危险示例回归检查。历史文字保留不是继续执行旧命令的授权。
- 证据：[本轮阶段](stages/windows-acceptance.md)、`tests/test_docs_site.py`。

## 外部依赖：网页可达与进程 API 联调必须拆分

- 日期：2026-09-13；标签：TLS、模型、授权、验收。
- 场景：用户授权远端用所给 key；网页抓取能见到 API 的 token-required 响应，而 Python/httpx 和 curl 在 TLS 阶段断开。
- 根因边界：尚未定位具体网络设备/服务方策略，不能断言是哪一方故障。它发生在 HTTP 前，不能判 key 权限，更不能用营销页上的名称替代模型 ID。
- 处理：带凭证请求只发到用户指定的 HTTPS 提供方；诊断再用不带凭证的请求。记录网络异常、协议层与未完成能力；不绕证书、不把密钥发到第三方代理、不循环消耗额度。
- 预防：显式探测工具拆开 models/chat/mermaid/embeddings，复用真实 LLMClient；默认测试只用 MockTransport。向量返回有效还须另测 FAQ 标定，Mermaid 前缀有效还须浏览器渲染。
- 证据：[本轮阶段](stages/windows-acceptance.md)、[新手指南](../Windows新手逐步验收.md)、`tests/test_probe_llm.py`。

## 能力与免费额度：失败不证明不存在，价格表不等于上线资格

- 日期：2026-09-13；标签：Agnes、embedding、诊断证据、免费服务。
- 场景：用户质疑“未找到文档”是否被说成“不支持”；带 Secret 的聊天成功，聊天 ID 的向量请求返回 HTTP 500，日志下载通道又受限。
- 处理：通过 CLI 已脱敏且转义限长的 notice 保留具体错误分类和各退出码，不读原始错误正文；明确 500、模型清单和文档缺项都不能证明整个提供方没有向量能力。绿色 job 仅涵盖设定的必要检查，不吞可选探测失败。
- 选型：分开记录开源本地推理无 API 费、云端免费额度、账号/地区/数据条款和实际集成效果；不把模型卡当成托管免费价格证明，也不在新手项目悄悄增加服务/依赖。
- 证据：[Agnes 阶段](stages/agnes-integration.md)、[候选与官方来源](../docs/AGNES_AI.md)。本轮沿用既有分层取证/确认边界，无需额外升级 Skill。

## 技能进化

### 2026-09-13：本仓库工作流 → v2.0

- 触发：此次交接/收尾规则审查发现实质性陈旧指令；不是仅因参考版本提高就复制升级。
- 参考：web_agent project-manager v13，固定提交 `4d518c1df1f1f2791235a8d49f64807b112af9fd`。采用阶段关闭/已解决 P1 的元复盘机制，而非自动模型学习。
- 旧版：本仓库三个 Skill 未显式版本化；新版统一声明 v2.0，版本号只属于本仓库。
- 权威源：`.claude/skills/codemax-workflow/SKILL.md`；同步副本：`manager/SKILL.md`；配套修订：finish-subitem、pre-commit-review。
- 行为变化：单一事实源映射、阶段/经验按需生长、授权外部服务分层证据、严守数据边界、元复盘→必要源修改→版本→同步→日志。
- 验证：源/副本逐字及提交示例回归见 `tests/test_docs_site.py`；本轮实际运行结果只记阶段页。
- 未做：不声称修改不可访问的全局 Skill 源/安装；不复制上游私密值管理方案；不覆盖 AGENTS 硬边界。
- 回看条件：出现新的流程诱发返工、阶段结构失控或多处状态冲突时再评估；无规则缺陷时记录“无需改 Skill”，不强制无意义版本上涨。

### 2026-09-13：健康检查覆盖全部现行 Skill，v2.0 → v2.1

- 触发：扩大收尾扫描又发现 schema-sync 的失效 HANDOVER 配方/固定数量，以及 new-tool-page 的旧静态 JS 路径、样式和 CDN 示例；只审三份收尾文件的范围不足。
- 源修改：codemax-workflow 增补全 Skill 审查范围并升 v2.1，manager 副本逐字同步；直接重审 schema-sync、新工具页和通用 FastAPI/pytest 约定，禁止仅凭 docstring 写测试或未经批准安装额外依赖。
- 实现边界：schema 对比还有时间列检查，但不等于完整 DDL 等价；重建两遍不等于无损升级。前端按实际 Vite 源/产物路径维护，不恢复旧 CDN。
- 验证：现行命令围栏检查扩至全部 Skill，额外拒绝固定 passed 数和旧 HANDOVER 编号；不是全局安装验证。
- 回看：以后阶段健康检查先扫描全部适用约定，再判断哪些需要语义更新，避免凭文件名缩小审阅范围。


## 2026-09-15：强回归套件仍需测试外部边界和用户取消意图

此次交叉审查确认：flush与持久提交不同、晚到响应会覆盖用户离开状态、输入经SQLite接受不代表PG可存、指纹不保证当前文案。新增故障注入/独立session读取/实际JS乱序/有界工作量反例。删除重复方案、保留历史证据，当前状态集中review台账。现行Skill已要求这些验证方法；另发现多命令调用中末尾tail可掩盖前序失败，因此本次只收紧退出码规则，不扩大业务。金融与迁移等未结项不得用本批CI绿色签收。详见 [当前审查](../review/README.md)。


### 技能进化记录：2.1 → 2.2（2026-09-15）

- 触发：一次文档测试真实失败，但后续tail使shell整体退出0；阅读失败摘要后修正了暂存状态，未将它交付为通过。此前部分补丁脚本中断也说明仅看最后命令不可靠。
- 行为变更：Linux批处理要求set -euo pipefail或显式逐项检查退出码，后台任务等待退出；不把该语法混入Windows CMD。
- 权威源 `.claude/skills/codemax-workflow/SKILL.md` 与分发副本 `manager/SKILL.md` 同步至2.2；其他Skill规则无需改变，未声称修改不可访问的全局源。
- 验证：cmp逐字一致，现有管理/文档门禁继续检查同步；四个独立进程内变异各须恰好pytest退出1才接受为有效反例。该文档约定不等于平台自动强制shell模式。
- 回看：若后续仍出现管道伪绿，再补可执行包装工具及其退出码正反测试，不堆空模板。


## 第二批元复盘与技能进化（2026-09-15）

初始化保护不能只改函数：Compose自动挂载、Docker排除SQL、当前目录.env、Windows教程都是实际调用路径。本批逐一同步；跨连接事务锁、失败回滚、签名有效但业务身份错误是关键反例。现行workflow由2.2升2.3、schema-sync由2.1升2.2，删除“当前full_init一定DROP”的过期前提，保留历史版本/测试清库警告，权威源与manager副本同步。其余Skill无需变化，未修改不可访问的全局源。后续回看完整schema对照、真实恢复与财务事件；当前证据集中[第二批](../review/RELEASE_BLOCKERS_PHASE2.md)。

第二批收尾教训：下单配置齐全不代表能处理回调；预支付前需验证本地平台凭据和解密密钥。补上这个门禁后完整重跑双后端，而不是继承补丁前全量结果；7种反例仅在测试进程内替换，避免与并行全量测试相互污染。

第三批教训：数据库事务测试不能让多个会话共用一条SQLite连接；原支付信息保护不能把不同流水吞成幂等成功；迁移基线不能跟随最新ORM增列而漂移；已购文件不是当前配置。按本批实际反例保存证据并按最终提交验证，不把触发器/文件快照说成防DB管理员篡改或完成财务合规。

第三批收尾发现同用户5次并发下单会先重复复制、把两槽挤满。改为复制前持数据库用户锁，外部网络前释放；完整双后端重跑而非放宽全200断言。路径保护必须用规范化后位置，不能只检查字符串前缀。

取锁的同值UPDATE也会触发SQLAlchemy的onupdate；显式保留update_time并用旧时间哨兵验精确重放。移除此保护的第10种反例被拒绝，最终功能再次双后端全量通过，细节集中第三批报告。


## 第四批元复盘（2026-09-17）

- 原“HTTP 200+二维码字段”未核验服务商应答；补验签后把旧格式负例也换成有效合成签名，避免所有坏JSON测试都在缺签名处提前失败。成功路径必须走真实密码学，不能只替身返回成功对象。
- 查询需要写审计，故本站入口用POST；远端GET不代表整个业务操作只读。开始先持久，成功audit与凭证同事务；不能事后补日志假装原子。
- 来源测试证明旧服务端没有Origin拒绝，不等于浏览器SameSite/CORS已被突破。改进范围明确为三种财务写，保留Bearer和无来源头非浏览器兼容，文档不夸大。
- 读取旧README时发现若干当前正文仍说商品未冻结/full_init删表；只追加新阶段会留下矛盾，本次直接改当前段落，不改写历史报告。
- 元复盘结论：现行v2.4已要求源码/调用方核对、真反例、文档语义和完整退出码，本批无需为新增功能强行升级Skill；源/manager副本保持同步。后续重点是证书轮换、退款/异常结案和真实浏览器验收，不通过测试计数掩盖范围。

第四批晚审发现HTML pattern使用Unicode Sets的v标志，普通JS正则中合法的字面斜线/连字符写法在浏览器表单中会失效。用Node v构造确认缺陷、修模板转义并加实际匹配回归；最终双后端重新运行，不沿用此前全量。另审计命中沙箱新建环境旧pip/setuptools，仅升级忽略环境中的引导工具，不新增应用依赖/忽略项。

## 第五批元复盘（2026-09-17）

ID有序不代表提交有序；复核摘要必须包含数量来发现低编号晚提交。孤立开始到龄也是新事实；投影GET不写状态，版本防另一位管理员覆盖同一轮。空筛选页仍有续页、新筛选不能继承旧游标、请求重试应保留原身份/正文都要执行JS测试。fixture碰到一人多个pending唯一约束，改成该用例真实需要的closed历史，不改约束。现行v2.4规则已要求这些反例与双后端，本次无需为功能新增强行升Skill；源/分发副本保持一致。环境恢复先逐文件核验已发布提交再恢复元数据，不覆盖文件或重做几百个历史差异。

## 2026-09-17 第六批元复盘

- 旧签名只绑定key/期限时，即使领链入口禁止退款单，已发出的链接仍可绕过；撤权必须进入实际文件出口，并在耗时校验后重读事实。无法召回已接纳传输/副本应明说，不能承诺无限撤回。
- 收款状态不是净资金状态。独立退款凭证保留原收款，迟到付款通知幂等，不自动恢复权益；PROCESSING/失败不能当完成，也不能覆盖已完成退款。
- 下载旧测试必须用真实订单领链后验证200/404/流式/过期，不能让统一拒绝旧链接掩盖原断言；退款合成数据构造与业务代码缺陷分别记录。
- 技能进化结论：工作流v2.4与schema-sync现有规则已覆盖源/调用者核对、独立连接、真实触发器和最终SHA CI，无需为新增领域规则机械升级；经验保留于此，源/分发副本继续同步。阶段证据：[第六批](../review/RELEASE_BLOCKERS_PHASE6.md)。

第六批额外反例：把同一退款时间改为丹麦+02表示，在SQLite精确重试实际409；其Datetime会丢偏移。付款与退款入库统一UTC，并新增原付款+08、退款+02的跨区重试/持久事实测试（manual/wechat各一），不能只测UTC输入就称可跨后端。

## 2026-09-18 第六批发布恢复与当前队列复核

- 本次恢复再次发现Git元数据回到旧基线，而工作文件保留；上次忽略目录/仓库外的bundle没有保留下来，不能将临时副本当作持久恢复保证。已对318个已发布文件核对，确认仅47处第六批修改与5个新文件，并核对全部精读及README指纹后才mixed-reset元数据；没有覆盖工作文件或重复提交累计历史。重新打包后立即推送，旧未发布SHA不作远端证据。
- 旧review首行仍称第四批“最新”，未结项表仍缺少第四批已交付能力。已按实际模型/API修正当前队列，并把可选第三方OAuth/云适配等与所选部署的必验项分开；只在末尾追加批次链接不能消除正文冲突。
- Skill结论：现有v2.4已要求恢复核对、立即推送、消除冲突状态源和精确SHA CI，无需机械升版；此处记录执行遗漏及修正，后续复核从当前队列而不是历史checkbox开始。


## 2026-09-18 第七批元复盘

- 外部协议字段决定能证明什么：退款通知缺channel，不能把此前查询要求的ORIGINAL偷填进去；先调整公开边界，再做可靠线索箱和显式查询衔接。不为了兑现过宽的初始描述削弱已验收凭证规则。
- commit前失败与commit后丢ACK都要注入，后一种必须原ID重放确认；签名新随机数不等于新业务事实。用同kind唯一约束防止同ID换状态绕过，通知/复核/金融成功分层。
- 前端便利按钮type=button、只填号不代替人工确认；迟到响应和部分退款的展示也需要源码/bundle回归。文档不把“收到SUCCESS”或4秒应用预算写成已退款/真实商户5秒SLA。
- v2.4已经要求协议取证、调用链核对、故障反例、语义文档和最终SHA全CI，无需机械升版；本次领域取舍记录TD-245。阶段证据见第七批报告，实际测试与尚未签收环境分开。


## 2026-09-18 第八批元复盘

- 先把“本地准备”和“向渠道发送授权”分开；稳定号解决本地丢应答，不代表外部未知退款闭环。未来发送器不能自动消费既有准备，内部审计依据也不能直接变成面向客户的reason。
- 新增0012撞到两处测试写死的未来0012编号；改成当前版本+1，保留故障DDL回滚和不重放断言，不降低迁移门禁。倒回历史基线的真库用例先按FK拓扑拆新表，历史SQL不改。最低所需版本仍应明确更新，不能因为目录缺新文件而假成功。
- 准备事实独立进入候选与资料摘要，避免只靠事件时缺记录就消失；保留旧无准备编码兼容。首笔/跨单/跨人/改变未知请求、提交前失败和提交后丢ACK分别验证。
- v2.4已有来源/调用者/双后端/反例/最终SHA CI要求，无需机械升版。阶段证据集中第八批报告，真实商户、Windows和部署操作不从合成测试推断通过。


## 2026-09-18 第九批元复盘

- 恢复时Git元数据回到旧基线；先对远端2931ff1的331个文件逐字节核对一致，才mixed-reset元数据，未覆盖工作文件。忽略环境重新安装，历史结果不冒充新验证。
- 钱款操作分开授权、完整不可变请求、持久尝试、已验签观察和成功凭证。超时不证明未发送；同本站尝试重放仅读，新显式尝试也必须复用原商户号/正文。没有消费者就不声称自动补偿闭环。
- 首轮测试误用既有签名验证helper/PG fixture参数；修正测试接线，不降签名或真库约束。新原因按UTF-8字节而非字符限长；配置改变不改已冻结正文。
- 现行v2.4已覆盖恢复、真实反例、双后端与最终SHA CI，无需机械升Skill。完整证据集中第九批报告，真实发送/部署仍未执行。


## 2026-09-18 第十批元复盘

- 再次遇到Git元数据旧基线；335个远端8c26afe文件逐字节一致后才mixed-reset，未覆盖工作文件。环境重新安装不意味着业务数据/历史验证已恢复。
- 自动后续流程前先补本地停止边界：停止和发送开始共用订单锁，不能承诺召回已有持久started的请求。不可覆盖原授权，独立停止事实也进入复核摘要，防只靠audit时消失。
- 首次定向验证达到工具180秒限制，没有退出码，不报通过；改用持久进程并等待完整203通过。有限测试同样可能超过普通命令窗口。
- v2.4已经要求锁顺序/独立连接/真实PG/完整退出码与最终SHA CI，无需机械升版；可靠通知后续核验及完整纠错/重新授权仍留队列，不能用本地停止代替它们。


## 2026-09-18 第十一批元复盘

- 338个已发布文件与28285e7逐字节一致后，仅恢复旧Git元数据；不覆盖用户源文件，不沿用上一批环境/测试成功。
- 只读可靠消费和资金结算授权拆开：现行RefundReceipt要求当前管理员，不让worker冒用账号。入站事务建任务，数据库时间+token/次数栅栏，领取前/网络中/落库前崩溃分别考虑。
- 测试接线首次误用签名helper参数及授权helper返回值；修正为实际调用契约，没有降低验签/回调断言。一个删除guard反例被后续NULL检查同样拒绝，属等价变异，未计检出；改用不可变创建时间实质反例。
- v2.4已有调用者核对、独立连接、反例、双后端/最终SHA CI要求，无须机械升Skill。系统自动结算/人工接管、真实商户和进程监督实机验收仍须分开推进。

## 2026-09-18 第十二批元复盘

343个已发布文件逐字节一致后才恢复旧Git元数据，未覆盖用户改动。人工接管避免使用“次数归零”：保留8次生命周期上限，用既有不可变事件绑定首次命令；同秒时间戳不能单独防ABA，需控制事件序号。服务端重放返回首笔操作而非当前状态，前端未知body不能随刷新自动改写。首次测试复用通知ID造成跨单409，改成明确独立ID而不是放松唯一约束；UI一次性脚本误重复执行后仅恢复本轮两个UI文件并单次生成、重建回归。现行v2.4已要求恢复核对/反例/双后端/最终SHA CI，无需机械升版；系统结算权限与真实部署仍未结项。
