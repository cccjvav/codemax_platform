# 第十六批：显式渠道关单与单笔核查衔接

## 范围与边界

基于5073e48，新增独立默认关闭WX_ORDER_CLOSE_ENABLED，管理员明确POST关原Native单；不改本地过期流程、不加worker/批处理，不发退款、不增schema/依赖，完整账本仍0017。

仅本站closed、无收款/流水、原商户/app相符、最新5分钟内可信NOTPAY才可新尝试。绑定原号/金额/查询attempt/key/内部依据，用户→订单锁；STARTED先commit再释放锁POST。所有应答先验平台签名/身份/时间及字节限制，只认204空体为ack。其他情况unknown，同key原人/内容仅读，不因配置/后续付款变化重发。未知新key必须在原start后新查NOTPAY且等待至少60秒，任何ack挡后续新关单。

finish只记原操作者观察，失权/付款竞争不会抹掉收款；迟到可信SUCCESS仍可closed→paid并唯一记账。管理页独立同意、未知完整body冻结、取消零POST、账号隔离、只读最新观察；新事件进入复核但不自动结案。**本项不是渠道日账下载、批量差异对账、手续费结算或服务取消**。

## 外部依据

本轮重新读取微信普通商户Native关单文档：https://pay.weixin.qq.com/doc/v3/merchant/4012791881.md （页面更新时间2024-12-11）。POST `/v3/pay/transactions/out-trade-no/{out_trade_no}/close`，body仅mchid，成功204无包体。生产验签沿用项目APIv3通用原始应答校验，不把业务错误/网络异常当未付款或关闭。没有用真实商户调用该接口。

## 本地验收记录

新后端32项、源码/bundle12场景；新后台+完整前端150通过，传输/管理旧组早期200通过。去新鲜NOTPAY、去首次actor绑定、把started提交改成仅flush三个受控反例各1项断言失败，字节恢复后新组32通过。首轮发现evidence子类字段覆盖丢掉控制字符pattern，已修复；配置fixture/参数/限流夹具问题按源码修正，未放宽断言。

全量SQLite **1587通过/6跳过**（1083.61秒），独立非超级用户、不可建库/角色的真实PG **1592通过/1跳过**（1198.87秒）。五项仅PG事务/迁移用例由真库执行；共同skip为未启用的真实embedding标定；passlib/crypt弃用提醒仍在。

Ruff、pip check、JS语法、README契约、文档站构建通过；前端重建与候选产物逐字节相同。npm审计0条，pip-audit严格模式沿用PYSEC-2026-1325例外、输出2 ignored，不声称零风险。人工更新源码解释后刷新指纹，186精读文件/2050段、291源码页、63业务路由；不是全仓语义自动认证。历史0001–0017逐字节不变。

发布CI另核最终SHA全部六项、远端tip和干净树，交付回复给精确运行链接，不继承第十五批绿色。新功能与现有查单/退款共用传输，旧200/对象协议保持不变，204只对关单显式启用。

## 后续

下一步渠道日账下载/差异核对、历史无凭证订单与服务生命周期；真实商户/TLS/代理/浏览器/Windows及备份恢复未代签。日账文件验证、隐私存储、差异分类尚未实现，不用人工复核或关单观察冒充正式对账结论。保持所有业务门禁默认关闭，没有业务库/真实退款或关单操作。
