# 第五批：异常复核待办，不冒充退款结案

状态：本地实现、双后端全量与反例验收完成；最终提交的六项CI以交付回复绑定SHA/run为准，外部未执行项不签收。第四批5609e8e的六项CI已独立核验全部成功：[35236186508](https://github.com/cccjvav/codemax_platform/actions/runs/35236186508)，不继承为本批通过。

## 实现与范围

- `/admin/payments`增加“需要复核 / 继续跟进”和“当前进展已复核”筛选，原“有历史异常”筛选保留原含义。
- 可以记录跟进、完成本轮复核、重新跟进；保留操作人/时间/说明。仅保存运营评估，不查银行、不确认到账、不执行或证明退款、不改收款凭证和下载权益。
- 使用现有PaymentEvent的`operator_review`类型和版本化JSON载荷，无新依赖/schema；不把JSON历史写回为可覆盖的状态字段。
- 基于当前事件数量与最大ID、订单状态/流水、凭证ID、超过60秒仍缺对应结果的开始记录生成摘要；旧复核仅覆盖当时看到的资料。新记录、低编号晚提交、付款和孤立尝试达到时限都会使旧完成标记过期。未知/无操作人的复核格式不算完成。
- 乐观版本与资料摘要挡住过期页面提交；用户/订单锁串行复核写入；请求ID绑定原单、发起人和完整内容，相同请求重试不追加，冲突409。网络错误不证明未写入，页面先刷新，保留原请求重试；账号切换清空资料。
- 队列每次最多筛200张候选、返回最多50张，可能返回空列表但仍有下一页；读取不写业务状态、不运行后台扫描任务。每批对最多50张订单做4次聚合读取，数据库历史量仍影响开销，不是恒定耗时或生产容量证明。

完整操作见[工作台手册](../docs/PAYMENTS_ADMIN_GUIDE.md)第五批补充。Windows继续按CMD、Conda和系统Node，不在业务库跑测试。

## 验证与中间问题

- 新增36项回归：28项复核HTTP/数据库/投影边界，原Node文件新增4个场景×源码/产物两份共8项。针对性测试已通过，最终SQLite 1117 passed / 6 skipped，566.13秒，exit 0；最终真实可丢弃非超级用户PG 1122 passed / 1 skipped，607.35秒，exit 0，集群已清理。SQLite跳过5项PG专用验证及真实embedding标定，PG只跳过后者；没有新加跳过条件掩盖缺陷。
- 10种独立进程/临时JS副本的受控反例全部被拒绝，每种恰好pytest退出1且核对具体断言：去掉资料count、去掉孤立数、跳过资料摘要、跳过复核版本、允许缺发起人完成、解除200候选预算、请求ID不绑定操作者、不绑定正文、丢响应换请求ID、更换筛选仍沿用旧游标。应用文件前后哈希一致，没有与全量测试相互污染。
- Ruff、README契约263文件/13所有者/0错误、69项文档测试、离线站158文件/1686精读段/0待补通过（不是语义认证）。重复Vite构建的管理入口哈希一致；源码和产物Node语法通过，npm audit 0漏洞，pip check通过。独立工具环境strict审计项目环境通过，仍仅既有PYSEC-2026-1325对应2条忽略，没有新增依赖或例外。Skill源/manager副本逐字一致，维持v2.4。
- 最终提交六项CI：待提交后逐项核验，最终SHA/run链接在交付回复绑定。
- 新分页fixture起初为同一客户造多个pending订单，被真实唯一约束拒绝；改为历史closed测试订单，未放宽约束或删除断言。复核记录缺发起人、只按最大ID判断、候选在查询中途已有结果和更换筛选误用旧游标均纳入边界审查。
- 恢复环境时Git元数据回到旧基线，逐个比对315个文件与远端5609e8e完全一致后，仅恢复本分支指针/索引，工作文件不被覆盖。不能把几百个恢复差异当作本批重写。

## 仍未完成

这是“一轮资料复核”，不是资金异常彻底解决。远程关单、退款申请/查询/回调、退款后权益策略、渠道日账对账、历史缺凭证财务解决，完整定制服务报价/里程碑/验收，以及真实商户、Windows/浏览器/恢复演练仍需后续。

没有认领/分派、到期提醒、外部通知或后台扫描进程；由管理员主动查看和跟进。现有快照恢复、Agnes默认、embedding关闭和动态Chromium停用保持。不将复核说明当作收款凭证、发票或不可篡改审计认证。
