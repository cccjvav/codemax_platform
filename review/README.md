> 第十四批当前：[发送前纠错与显式重新授权](RELEASE_BLOCKERS_PHASE14.md)。0017；旧版先停止、本站从未开始发送且无退款观察，才可同号同额建立新授权；不发送资金。

> 第十三批历史：[系统自动登记授权与审计](RELEASE_BLOCKERS_PHASE13.md)。默认关闭；仅独立验签全额原路成功查询可登记，不发送资金、不冒用管理员；需0016。

> 第十二批历史：[核验任务人工接管与剩余次数重排](RELEASE_BLOCKERS_PHASE12.md)。暂停仅此任务；不清零次数，耗尽/成功仍人工查询。

> 第十一批历史：[退款通知持久核验队列](RELEASE_BLOCKERS_PHASE11.md)。默认关闭的独立worker仅GET核验；SUCCESS观察仍待管理员独立确认，不自动退款或撤销下载。

> 第九批历史：[独立授权、固定请求与显式退款发送](RELEASE_BLOCKERS_PHASE9.md)。发送默认关闭；准备/授权不会自动发送，申请观察不是成功凭证。

> 第八批历史：[第八批本地退款准备台账与幂等恢复](RELEASE_BLOCKERS_PHASE8.md)。当前剩余项与下一步以本页“后续优先顺序”为准；各批原始测试结果保持历史口径。

# 2026-09-15 交叉审查与修复台账

## 范围与结论

用户授权重新审查、交叉验证两份报告、修复错误，并整理旧报告/方案。输入为上传提交 `f7ae4a3` 中的两份原文，均审查应用基线 `889e3eb`：

- [报告 A：问题清单与优化建议](01a08bf5-review-report.md)
- [报告 B：完整只读审计](CodeMax_Platform_全面只读审计_01a08bf5_2026-09-15.md)

本页前半部分保留最初交叉审查记录，后半部分维护当前剩余项；两份输入原文保持不变，不把报告自身的结论当成已复现事实。评估依据包括当前源码/调用方、已有测试以及新增 `tests/test_audit_20260915.py` 的故障注入与实际 JS 执行。

**已完成第一批明确缺陷修复，但全项目审查尚未结项，不具备真实资金或面向不可信第三方 OAuth 的上线签收。** 第一批不新增依赖或数据库 schema，不切换 Agnes，不打开 embedding、不启用动态 Chromium，不改“已付款用户可以重新领链接”的产品规则。

## 第三批进展

[资金凭证、预支付尝试与固定文件权益](RELEASE_BLOCKERS_PHASE3.md)：已实现唯一流水与原子凭证、本地快照及旧单核准；自动对账退款、完整运营界面和真实环境仍未完成。下方第一/二批为历史证据。

## 第二批进展

用户已授权继续处理剩余阻断项：[初始化/迁移、支付信任与第一方SSO边界](RELEASE_BLOCKERS_PHASE2.md)。以下已修表与验证表保留第一批历史证据，当前未结项表同步第二批实现。第二批本地全量SQLite 933/6skip、PG 938/1skip，43项新增回归、7种反例被拒绝；详细边界见该报告。

## 已核对并修复（第一批）

| 项目 | 对应报告 | 交叉结论与实际修复 | 主要回归证据 |
| --- | --- | --- | --- |
| 限流器二次复杂度 | A P3、B H-04 | 确认。每次最多轮转检查 32 个 key，每 key 一条维护记录；默认最多 16384 个桶，满时拒绝新 key 而不驱逐活跃配额。过期按各桶自己的窗口；仍是单进程限流 | NoScan 字典禁止请求路径调用 items；容量/活跃桶/过期/混合窗口/清理测试 |
| 请求 ID 与日志伪造 | A P3、B H-05 | 确认。只接受 1–128 位安全 request-ID，否则生成替代值；路径/方法限长并转义控制字符，不记录查询参数。Docker 禁用 Uvicorn 重复 access log | 超长 ID、编码 CR 的 HTTP 回归；代理日志仍须单独设置 |
| 登录用户名 NUL | B M-03 | 确认数据库边界缺口。NUL/超长用户名不进入查询，返回统一 401，并保持未知用户的 dummy 校验 | HTTP 登录回归，随后在 PostgreSQL 套件复验 |
| 图表 NUL 与版本头 | B M-01 | 确认。name/content 拒绝 NUL；If-Match 仅接受有界 ASCII 单版本，可裸数字或强引号，超长/重复引号/弱标签/列表/越界均 400，不回显原始长头 | API 422、超长及非法 ETag、正常版本形式测试 |
| 图表列表拉取全文 | B M-01 | 确认。load_only 只选摘要列，不把 XML 取回后才从响应删掉 | SQL 执行事件断言 SELECT 不含 content |
| 回调 UTF-8、结构和字节数 | A P1-2、B C-02B/M-06 | 确认。流式累计最多 65536 字节；无效 UTF-8、非对象 JSON/resource/解密结果、非法订单标识和金额形状分别受控失败，不落入数据库 500 | 原有真实密码学合成测试保留；新增边界与解密结果形状故障注入 |
| 预支付早于本地持久提交 | B C-02A | 确认，flush 不等于 commit。现在先提交订单，才调用预支付；失败保留 pending、重试复用同一单号，使用订单已保存金额/名称；返回前刷新，以看到并发回调状态 | 提供方桩从独立 session 读到订单；commit 故障时提供方零调用；失败后两次同单号、库里只一单 |
| 生产链接的 Host 派生 | B H-02/M-04 | 确认。生产 public_base_url 使用配置的 canonical origin；启动严格检查 HTTPS host/端口，拒绝 userinfo/path/query/fragment/控制字符。开发仍按请求地址生成可用本地链接 | 恶意 Host 不影响生产链接；无效 origin 参数化测试。不是新增 Host allowlist |
| 支付模式自由字符串 | A P3、B H-02 | 确认，Settings 改为 wechat/mock/manual Literal；路由保留防御校验 | 原配置/商店/启动测试保留 |
| 商店取消被旧响应覆盖 | B H-12 | 确认。返回首页递增 buySeq、清当前单号/定时器/待登录补单订阅；旧响应失效 | 源码与构建产物均执行延迟 poll→取消→返回场景 |
| 客服 UUID 与管理员误发 | A P1-3/P3 | 确认。randomUUID 缺失时先用 getRandomValues，再提供仅用于幂等 ID 的 UUIDv4 格式随机降级；生成在 try 内。管理员未选客户时按钮禁用且提交逻辑拒绝 | 删除 crypto 后发送失败重试仍用同一合法 UUID；管理员未选择时 POST 数为零 |
| 管理列表乱序 | A P2-10/P3 | 确认。客服 inbox 与 Draw.io manage 增加独立请求序号，只接受最新响应 | 两份前端源码与 bundle 均执行新响应先到、旧响应后到的测试 |
| Draw.io JSON 与 Word URL 生命周期 | A P2-10/P3、B M-06 | Draw.io 把无效 JSON 转为有界可读错误；ER Word 下载延迟释放 Blob URL，与现有 Draw.io 做法一致 | 既有前端生命周期回归；不是 Firefox/Word 真机认证 |
| SQL 信息损失/幻影表 | A P2-7、B H-07 | 修复 TEMP/TEMPORARY/UNLOGGED、dollar-quoted 文本中的假 DDL、嵌套块注释、带逗号标识符的复合键、常见多词/数组/限定类型和括号 DEFAULT。仍非完整方言解析器 | 正反语料与原 SQL 解析套件；剩余限制见下节 |
| CI 与文档不符 | A P2-4/9/11、B H-11/M-10 | 增加 npm audit（high/critical 阻断）、手写 JS 语法检查；六 job 明确超时、checkout 不保留 Git 凭据。修正“重建两遍＝幂等”和产物命名说明；不擅自升级依赖或做全库格式化 | 原 CI 合同、文档构建、npm 审计/重建以及最终提交的实际 CI |
| 活跃文档/注释漂移 | A P1-1/P2-5/6/11、B M-10 | 根 README 去掉易漂总数、旧 er.js/行号机制和“支付闭环/云存储已完成”等过度声明；测试名称与重领语义对齐；阈值说明不再引用其他模型的未经标定经验值 | README/源码讲解/链接门禁；历史日期下的测试数字不改写 |

预支付失败的旧测试要求“库里没有订单”，会重现被审查指出的孤儿支付风险。因此这项断言不是被删掉，而是替换为更强的新合同：失败后持久 pending、同单号重试、不得重复建单，并另测提交前失败不得调用服务商。**保留订单不等于完整的查单对账恢复。**

## 没有照搬的建议与评级

1. A 建议非法 UTF-8 回调直接 200。本批不采纳：它尚未验签，不能确认已成功处理；返回有界 400，过大返回 413。只有已认证且按当前业务处理/忽略的事件才能 SUCCESS。完整商户回调政策仍需真实联调。
2. 收款码并非刚发现的未知秘密：`app/config.py` 和 TD-225 有此前用户上传/确认记录。本批不删除、不轮换、不改变默认收款人；它是支付用途和上线政策问题，不是已经确认的 API 密钥泄露。
3. 不给 Draw.io 的任意 autosave/save 事件自动设置 ready。现有 origin/source/epoch/load/export 相关性有真实作用；不能为了“兜底”取消时序保护。仍需浏览器协议/脏稿验收。
4. CPU 池创建竞态未在当前调用链成立：`_get_executor` 由同一事件循环同步调用，创建段没有 await，没有线程调用方。删除重复 import，但不为尚未证实的线程竞态添加锁。
5. 根 README 的统计漂移确实存在，但不等同于可直接接管账号的 P1 安全漏洞。历史验收节有日期时，旧数字是有效历史证据，不全部改成今天的数字。
6. GitHub 显式标记触发是此前在 workflow_dispatch 权限不足时经用户授权的后备。不直接删除可用测试入口；普通提交不含 marker，不调用 Agnes。它依赖分支写权限信任，仍不是环境审批或花费预算。
7. “没有发现明确 SQL 注入/SSRF/XSS”等报告结论只能限定于其范围，不代表以后无需复查；也不能把未证实的攻击宣称为已利用漏洞。

## 未结项与发布阻断（不得当作已修）

| 主题 / 报告编号 | 当前确认或待验证状态 | 下一步 |
| --- | --- | --- |
| 默认种子与生产初始化 C-01、H-01/H-02/H-08/H-10 | 已实现无种子/拒绝覆盖init、显式bootstrap、0008接入/0009停用公开凭据、校验和账本/事务锁与生产启动检查。Compose仍是隔离示例；早期升级、完整DDL等价、生产最小权限/备份恢复未验收 | 现有0008升级按第二批指南；继续补早期升级/完整结构对照与真实恢复，不把部分基线检查当全面等价 |
| 完整资金与权益 C-02B–E | 第三批已补尝试/凭证账本与交付快照；第四批已补预支付/查单应答验签、主动查单与管理页；第五批已补复核待办；第六批已补单笔全额退款核验/人工已退款登记及订单下载撤权。第七批已补可信退款通知入站与待核验衔接；第八批已补本地全额准备；第九批新增显式授权/冻结请求/默认关闭发送与同尝试恢复；第十批已有不可覆盖的后续本站发送停止；第十一批已有默认关闭的持久只读核验；第十三批已补默认关闭的系统成功登记；仍缺部分退款记账与纠错、远端关单、渠道日账与服务交付流程 | 显式发送与原号未知恢复已实现；已补发送前显式重新授权；下一步运行恢复与进程监督（发送后纠错仍未支持）；普通交易REFUND或人工复核完成不能代替退款成功凭证。真实收款上线仍须单独签收 |
| OAuth H-03、会话/CSRF M-02/M-03 | 第二批已在production默认拒绝未显式信任的第一方客户端，要求HTTPS回跳；仍不能当最小权限第三方授权服务器；缺 scope/audience、PKCE、一次性同意事务和会话治理；风险取决于客户端信任与部署 | 当前保持受信第一方边界，补真实浏览器验收；若以后开放第三方才另行设计 grant/令牌/同意/撤销，不把可选扩展误列为现有第一方方案必装组件 |
| 爬虫 H-06、LLM/RAG M-05、容量 H-04/M-13 | 本批未复现所有资源/重定向政策探针；已有 SSRF 固定 DNS 与动态浏览器禁用保留。报告所列 robots 每跳、状态淘汰、总 deadline、模型响应字节/预算、全文章扫描值得专项处理 | 独立有界资源/重定向回归与负载试验；不得只清本进程缓存就声称多实例支持 |
| SQL H-07 剩余 | ALTER 外键、隐式父主键、未引号标识符折叠、部分 schema/类型/表注释等仍未完整支持；产品输出仍须人工核对 | 确定狭窄语法拒绝策略或选正式方言解析器；新增依赖先确认，不能把部分修补称作完整 MySQL/PG 支持 |
| Draw.io H-12、M-01/M-09 | 尚无完整脏稿提示/草稿恢复、服务端 XML 子集校验、全部删除/恢复前置条件；仅修部分列表/错误边界 | 保留账号隔离优先；专项设计不会把旧账号草稿泄露给新账号的恢复路径，实际浏览器验证 |
| 日志/就绪/运维 H-05/H-10、M-07/M-08 | Docker 重复 access log 已关，但其他 Uvicorn 启动和代理须自行禁用/脱敏查询；没有不可篡改审计、真实告警、备份恢复、完整 feature/schema readiness。静态缓存/压缩仍待测 | 统一运维日志政策、真实代理与容器验收；不把 liveness 改成外部服务探测，也不对固定资产盲加 immutable |
| 隐私/来源/许可证 H-09、M-11 | 尚未确认具体违法或许可违规，但对外处理告知、文章来源审核/保留、销售/退款/学术诚信条款、项目许可与第三方 notices 确有待办 | 由所有者决定许可/商业政策；不自动添加开源授权或声称模板即合规 |
| 工具与流程 H-11、M-06/M-12、L-01–L-10 | 仅补明确 CI 缺项；action/image digest、依赖 hash lock、容器/浏览器/迁移链、静态类型、日志评论权限、格式化、索引/连接池/密钥缓存等未全部实施 | 依风险和基准增量推进，不为“优化”堆新依赖、删除有效测试或篡改产物 |

## 旧资料整理

- 两份新审查原文移动到本目录并登记文档站，保留字节内容与上传来源，不作为执行指令自动接受。
- 删除根目录重复的《代码级文档方案新/旧》原稿：它们的取舍已经整合进 [文档执行契约](../docs/DOCUMENTATION_POLICY.md)，不再需要并列两套提示词。原文仍可从 `889e3eb` 的 Git 历史恢复。
- 保留带日期/SHA 的旧审查与验收记录：它们记录当时的证据和迁移边界，删除反而失去追溯价值；文档站明确归入历史记录，不作为当前操作入口。
- HANDOVER、README、ROADMAP 指向本页；当前函数说明仍在目录 README/精读数据，不复制另一套代码说明书。

## 第一批验证（f7e1cdc，历史快照）

以下是本批最终功能实现的 Linux 沙箱证据；测试/注释最终整理后又跑了相关与文档组（137 passed / 1 个 PG 专用 skipped）。新增回归 58 项，定向审查/SQL/配置/文档组 191 passed。

| 验证 | 实际结果与边界 |
| --- | --- |
| 全量 SQLite | **890 passed / 6 skipped**，391.67 秒；其中 5 项需 PG，1 项需真实向量标定 |
| 全量真实 PostgreSQL | **895 passed / 1 skipped**，433.37 秒；pgserver一次性库、非超级用户数据库所有者，结束后已清理；包括真实事务并发及现有迁移保留数据回归，不是全部历史升级路径证明 |
| Ruff / pip check | 通过；未新增应用依赖或 schema |
| pip-audit --strict | 沿用既有 PYSEC-2026-1325 例外后通过；未扩大忽略名单。当前数据源将同一 ecdsa 0.19.2 通报重复列两条，所以输出为2 ignored；不加忽略时退出1，两条都指向同一ID且无修复版本，不是“零已知漏洞” |
| npm audit | high门槛通过，当前报告0条漏洞；版本/锁文件未改，不是永久安全保证 |
| Vite / JS | 全部产物重新构建；node --check通过。源码与bundle执行取消、UUID、未选会话、乱序列表回归；不是Windows/浏览器/外部iframe/Word验收 |
| 文档契约 / 站点 | 门禁与构建通过；人工解释先更新，随后绑定源码SHA/连续段界，再刷新README指纹；移除提案页、登记review并校验链接 |
| 有控制的变异 | 内存移除限流容量检查→1失败；移除预支付前commit→1失败；撤销商店取消序号更新→1失败；恢复SQL逐$尾部切片→1失败。各进程独立，磁盘实现未被改坏 |
| 资源检查 | 同一沙箱固定时钟下20000次限流尝试约0.125秒，最终16384桶；不与其他报告不同环境的秒数计算加速倍数，真正回归依据是容量/禁止全扫/禁止尾部复制断言 |

两套全量均有1条既有passlib/crypt弃用提醒。未做新的真实商户、Windows/Conda、浏览器/Word、代理/容器、外部OAuth或真实embedding验收。此前Agnes证据仍归其独立阶段，本轮没有再次发起带凭证模型请求。

**远端CI追溯**：推送后必须逐项确认包含本页的最终提交的六个job。不可变run链接在交付回复提供；可用 `gh run list --branch arena/01a08bf5-codemax-platform` 找到对应headSha，再用 `gh run view` 逐job核对。本地结果不替代远端Checks，后续提交也不能继承本批绿色。

## 元复盘

根因不是“缺少一份报告”：完整清单/指纹与大量测试仍会漏掉跨事务、跨环境和异步用户意图边界。本次增加独立 SELECT/故障注入/晚到响应反例；活动文档去掉手写总数，历史证据保留。现行 Skill 已要求这些验证方法；另把实际遇到的“末尾 tail 掩盖中间失败”提炼为退出码规则，权威源/副本由 v2.1 同步到 v2.2，其他 Skill 不机械升级。后续把金融与迁移等未结项留在明示台账，不用绿色单元测试代替发布判断。

第五批：[异常复核待办与验证](RELEASE_BLOCKERS_PHASE5.md)。第四批5609e8e六项CI已核验成功，本批仍独立验收。

第六批续做入口：[退款凭证与订单下载权益](RELEASE_BLOCKERS_PHASE6.md)，范围、验证和剩余退款/服务边界单独记录。

## 后续优先顺序（2026-09-18，当前队列）

这是尚未完成的工作，不是另一次“已交付”声明；各批最终提交的六项CI在对应交付回复单独核对。只把当前源码确实缺少的流程放入队列，已经实现的响应验签、主动查单、管理页、站内人工客服和全额退款核验不再重复列成待开发。

| 顺序 | 剩余工作 | 完成时必须证明什么 | 执行边界 |
| --- | --- | --- | --- |
| 1 | 运行恢复、发送后纠错与部分退款（发送前重授权已补） | 已实现独立授权、冻结请求、显式发送及未知原号恢复，默认开关关闭；已有只读核验消费者，但不能自动发送历史准备/授权。已有永久停止新本站尝试及停办依据（不能撤回渠道）；已补独立默认关闭的系统登记；已补发送前正文纠错/重新授权；继续补运行恢复与进程监督，发送后纠错另设计；查询/回调重复或乱序不重复记账；申请、受理、未知、实际成功分别记录，部分退款权益另定规则 | 先用合成渠道与可丢弃数据库开发测试；真实转账/业务库操作另行授权，不在普通测试中执行 |
| 2 | 远端关单、渠道日账核对、历史无凭证订单处理 | 区分本地过期与渠道关单；迟到成功不能丢付款；差异和旧数据可追溯，不能捏造历史收入 | 本地关闭代码已经存在，缺的是渠道流程及对账；不能用改status替代 |
| 3 | 定制开发/服务的报价、合同、里程碑、交付与验收 | 客户确认后的范围/价格版本固定，付款与阶段交付关联，取消/退款不冒充服务已经履约 | 当前商城是固定文件订单；站内客服已可由你本人接待，但聊天页不等于完整服务订单系统 |
| 4 | 数据与部署验收 | 更早迁移路径/结构等价、真实备份恢复、代理与持久化、告警、容量和重启后权益恢复 | 预发布可丢弃副本先演练，不能在业务库运行pytest或重跑full_init；已有真PG测试不替代恢复演练 |
| 5 | 实机与经营边界 | Windows的VS Code CMD/Conda、浏览器/Word/Drawio、真实商户与第一方SSO都有记录；销售/退款/隐私/文章来源/许可条款与实际服务一致 | 自动化、文档步骤和CI绿色不是这些项目的实际签收；许可及商业承诺由所有者决定 |

另外保留工具专项待办：SQL方言剩余语法、Drawio草稿/恢复与XML边界、爬虫/模型资源上限及容量验证，见上方对应行。没有把它们因支付修复而自动勾选完成。

**可选扩展，不默认混入当前上线范围**：OSS/COS云适配、多实例全局限流、开放第三方OAuth、更多商品目录、外部通知/开票服务。当前可以明确选择LocalStorage、单实例、受信第一方；选了哪些能力，就验证哪些能力，不对外宣称未实现能力。Agnes继续使用，embedding在确认模型能力和标定前保持关闭，动态Chromium不启用。
