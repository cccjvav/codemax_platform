# 第十四批：发送前纠错与显式重新授权

## 已实现范围

仅最新授权已停止、本站全单从未持久开始发送且无退款查询/通知/核验/成功事实时，允许以相同准备号/全额新建显式后继授权。客户原因/当前正式回调重新冻结，旧版/停止/首次操作者和时间不覆盖。没有改变付款、金额、退款号或下载权，不发起真实资金。

0017唯一根+唯一后继自外键，应用和PG约束同准备/已停止/无活动/只追加；用户→订单锁。新授权与审计同提交，同key先按首次人/内容恢复，晚到响应不重写正文。发送仍需当前叶子、既有默认关闭的SEND和单独管理员确认；旧尝试/停止与初始授权按显式原版本读回。started即使提交ACK丢失且尚未HTTP，也不能当未发送纠错。

管理页新增独立表单和有界50版历史，unknown保留完整body/key，切账号清屏/拒迟到；复核摘要包括所有授权与停止，不受事件翻页或缺过程审计影响。本次不处理发送后、金额/部分/成功凭证纠错，不承诺外部商户没人办理；固定原号始终保留。

## 验证记录

新增后台最终29项与前端源码/bundle10场景；早期系统+前端134通过。三个受控反例（去停止条件、去重放actor绑定、审计前提前commit）各1项断言失败，逐字节恢复；恢复后新组/维护/ER/Word53通过。全量SQLite **1498通过/6跳过**（1017.44秒）；独立非超级用户、不可建库/角色的真实PostgreSQL **1503通过/1跳过**（1148.32秒）。五项仅PG隔离测试在真库执行，另一个跳过是未开启的真实embedding标定；未启用付费模型或真实退款。升级参数化验证原0016已停止/未停止两种历史，逐列核对授权与完整停止行不变，并验证不能借用另一准备的停止父版本。

首轮全量有两项文档门禁失败：新增端点鉴权/限流计数未同步，以及新报告未暂存导致tracked集合不符；修正后完整重跑通过，不降低断言。补测时时间列名误写create_time，按真实created_at修正，测试证明迁移前后的原时间保留。

Ruff、JS语法、README契约、文档站构建均通过；前端重建与提交候选产物逐字节一致。最终测试排版调整后新后端+文档门禁74通过；npm审计0漏洞，pip-audit严格扫描保留既有PYSEC-2026-1325例外，报告2 ignored，不声称零风险。精读182文件/1997段，287源码页，62业务路由；这些是定位/覆盖统计，不是全项目逐行语义认证。本报告记录本地验证；发布CI须对应最终提交的六项job，交付时另附精确运行链接，不继承第十三批绿色。

首轮旧停止响应断言因新增history/reauthorize_allowed动态字段不符，补完整字段预期；新组发现孤立查询终态需保守阻断，已修正并测试。全文建库SQL超过公共ER/Word文本预算，改公共夹具只投影全表DDL并断言原图完全一致，不抬生产限制，不裁表/字段/外键；真实PG另验过程触发器。

## 运维与后续

现行0017，停止全部旧写实例、备份并核实恢复后原CLI migrate/status、一致更新Web和独立worker。旧0001–0016不改，已有后继不能删列回退单授权模型；未操作业务库或开启任何生产开关。操作见管理/部署/Windows新手指南，SQL降级helper仅为一次性测试旧基线。

后续优先进程监督/报警与运行恢复演练，再渠道关单/对账及服务生命周期；发送后/部分/凭证纠错另设政策。Windows/浏览器、真实商户/TLS/代理/备份恢复未代签。元复盘在manager/experience，v2.4保持，不机械升Skill。
