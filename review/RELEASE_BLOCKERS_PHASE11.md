# 第十一批：退款通知可靠只读核验队列

## 交付范围

0015持久任务与通知同事务ACK，重复不重置；独立默认关闭worker每周期补最多50个旧任务、CAS领取、90秒租约、数据库时间/token/次数栅栏，数据库事务不跨GET。租约恢复允许重复GET，不保证恰好一次。PROCESSING/未知失败指数退避最多8次，终态/部分/冲突/耗尽转人工；不记录原始账户/签名包/失败正文。

管理页独立展示最近50任务及截断提示，带原退款号、状态、次数和时间；账号切换清屏、旧响应丢弃。停止本站发送不阻断独立核验。没有新应用依赖、真实网络查询/退款、业务库迁移或部署操作。

## 权限边界与未决

**SUCCESS观察不自动创建退款成功凭证，不自动撤销下载。** 现行凭证需要当前管理员，worker不能冒用管理员；通过原号人工独立查询才结算。系统自动结算/人工接管权限、完整纠错/重新授权、部分退款、渠道关单/对账及服务生命周期仍未完成。没有重置attention/次数的接口或外部提醒；开关true不证明进程在线。两类退款开关继续默认false。

## 验证结果

| 验证 | 本批实测 |
| --- | --- |
| SQLite全量 | 1390 passed / 6 skipped / 1 warning，914.31秒，退出0 |
| 可丢弃真实PostgreSQL 16.2全量 | 1395 passed / 1 skipped / 1 warning，1029.06秒，退出0；角色非超级用户/不可建库/建角色，集群清理完成 |
| 新增回归 | 23项后端/迁移、6项源码/bundle前端，共29项；定向101通过 |
| 控制反例 | 9项实质变异均产生测试失败，源文件恢复后再跑完整双后端 |
| CLI额外烟测 | 独立非超级用户PG，完整账本/空收件箱--once退出0；篡改0015校验和退出1；无商户配置/网络，集群清理 |
| 文档/前端 | 收尾147通过；README契约282文件/13所有者/0错误；177文件1924段讲解，124模块521依赖边60路由1687符号53份文档 |
| 静态/构建/审计 | Ruff、JS语法、pip check通过；Vite重复全部JS摘要一致；npm audit 0；pip-audit --strict沿用PYSEC-2026-1325例外通过（2 ignored），无新增应用依赖或豁免 |
| 发布约束 | 历史0001–0014字节未改，Skill源/分发副本相同；仅固定分支发布，最终提交六项CI按精确SHA在交付消息关联 |

反例分别是：绕过默认关闭、忽略租约期限、部分通知仍查询、错商户仍查、任务提前提交而审计失败、取消重试耗尽、丢失入站任务、接受缺0015迁移包、允许修改任务创建时间。删除guard的等价变异因后续NULL归属检查仍拒绝而未计检出，不冒充测试发现缺陷。

首次测试误用旧签名helper/授权helper返回值及前端测试调用方式，修正真实接线后通过，没有放松验证；文档登记测试须先将新报告纳入索引，已解决。两套全量各一条既有passlib/crypt弃用警告，跳过项不计执行。Node VM/合成签名/可丢弃库不能代替真实浏览器、Windows、商户、TLS代理、进程监督或备份恢复签收。

补缺每周期最多50条仅限制结果/写入量，不是数据库扫描页数上限；历史增长仍需实测计划/负载。没有全局限流/吞吐SLA/外部报警/attention重置接口；管理员需监测积压与过期租约，原号人手接管，不能删事实或换号重退。开关仍默认false，本轮未启动实际部署worker。
