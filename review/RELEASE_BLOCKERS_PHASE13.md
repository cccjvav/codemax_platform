# 第十三批：系统自动登记授权与审计

## 范围与边界

WX_REFUND_AUTO_RECORD_ENABLED默认false，与VERIFY/SEND独立；只在本轮独立验签GET取得原收款全额ORIGINAL/CNY SUCCESS后，授权登记已经完成的退款。通知、申请受理/POST SUCCESS或支付REFUND均不够。不发送资金，不造User或借管理员，人工入口仍核活跃管理员。

0016保留旧凭证，系统actor_id为空、固定system:refund-verifier、唯一verification_event_id指向开始事件。SQL检查活租约/同单系统事件，原全额/只追加保护不变；SQL不代替外部验签。应用按订单→job锁重绑配置/原收款/通知/出站参数与结果；保存点处理预期冲突，末次再读数据库时钟，凭证+终态审计+任务一次提交。hold/过期/旧token不得落凭证，网络不持锁；下载按原订单阻断，原付款/其他订单保留。

同事实重试保留首笔归属，冲突attention/receipt_conflict不覆盖。最多8次GET/退避/人工控制不变；已有verified/attention不会因启用新开关自动复活。没有部分退款/纠错重授权/渠道取消/预算扩展、真实资金或业务库操作。

## 验证记录

| 检查 | 本批实测 |
| --- | --- |
| 最终SQLite全量 | **1459 passed / 6 skipped / 1 warning**，995.40秒，退出0 |
| 最终可丢弃真实PG16.2全量 | **1464 passed / 1 skipped / 1 warning**，1114.39秒，退出0；确认NOSUPERUSER/NOCREATEDB/NOCREATEROLE，集群已清理 |
| 新增回归 | 34项：系统组28项与源码/bundle新增6场景；系统定向28通过，46.56秒 |
| 收尾定向 | 前端/ER/文档171通过，15.88秒；补齐Word夹具后Word/ER/文档80通过，8.78秒，再完整重跑得到上述全量结果 |
| 真实生产结构冒烟 | 独立非超级用户PG经full_init+完整账本/全部触发器，真实worker.run(once=True)检查账本，合成RSA签名GET到系统凭证/终态审计/任务同提交；重复零GET。一次性脚本额外验证，不冒充业务库或真实渠道，集群已清理 |
| 静态与构建 | Ruff、JS语法、pip check、README契约通过；前端重复构建字节一致，文档重复构建357文件一致 |
| 文档 | 285源码文件/13归属目录/0契约错误；180文件1970段精读、55文档及61路由。范围/指纹通过不等于整库语义认证 |
| 依赖与范围 | npm audit 0；pip-audit --strict沿用PYSEC-2026-1325例外通过（2 ignored），不是无例外零漏洞。无新增应用依赖或豁免；0001–0015迁移字节未变；Skill源/副本相同 |

既有passlib/crypt弃用警告和跳过项不掩盖、不算执行通过。CI必须在推送后按最终完整SHA核对全部六项，具体SHA、run及结果由最终交付消息给出；此处不预填成功，不借用第十二批CI。

三个实质受控反例：跳过AUTO授权、移除暂存后的末次DB时钟读取、跳过最终商户重绑定，分别触发对应1项断言失败，原文件逐字节恢复。另一个只删state/token的变异仍被空租约防线拒绝，未计检出，不将其冒称覆盖全部栅栏。

## 运维与未执行

现行完整账本0016；原0001–0015不改。备份/停写/恢复核实后用原维护CLI migrate/status，Web和独立worker一致部署/分别重启；默认保持三个开关false。本批不执行业务迁移、开启开关或真实退款。详细说明见管理/部署手册与Windows新手逐步验收。

Node VM/合成RSA/AES和可丢弃PG不等于Windows、真实浏览器/商户、TLS代理、进程监督/告警或备份恢复签收。下一批优先完整纠错/重新授权及运行恢复，之后渠道关单/对账与服务生命周期；部分退款须独立政策，不直接放宽金额条件。元复盘见manager/experience.md，现行v2.4规则足够，无机械升Skill。

首次全量在ER布局夹具遇full_init新增后超20000字符API预算；中止而不称通过。仅夹具移除独立注释并追加全图等价断言，不提高生产预算、不删结构或弱化布局断言；定向5通过后重跑全量。

随后完整双后端发现Word导出夹具同类超长422（各1失败，SQLite1458/PG1463通过），未称全绿；补查全部full_init调用方，仅调整Word夹具且新增全图等价断言，再完整重跑。
