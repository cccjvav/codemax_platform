# 第十二批：核验任务人工接管与剩余次数重排

## 范围

本批无新schema/依赖，仍0015。新增本地控制端点及第十一类表单，用户→订单→任务锁；首次命令与任务原子提交，key绑定原任务/人/内容。同请求仅读首次动作，不承诺任务现在仍处于该动作结果。snapshot加入控制事件序号抵抗同秒ABA。hold栅栏在途旧结果但不能召回GET、不暂停其他任务；retry仅剩余8次总预算，不清零、不换号、不发退款或改变下载权。已verified/耗尽/部分/已有凭证不能重新入全额自动队列，仍按原号人工查询。

未知响应保留完整body/key，409重新核对，账户切换清屏并拒绝旧响应；最新控制事实与任务投影不随事件分页消失。原管理员查询仍是成功凭证入口，系统自动结算/扩预算、外部提醒和服务监督未完成。没有真实商户、业务库或部署操作。

## 验证结果

| 检查 | 本批实测 |
| --- | --- |
| 本地SQLite全量（预算回归加入前） | 1424 passed / 6 skipped / 1 warning，932.12秒，退出0 |
| 本地可丢弃真实PG16.2全量（预算回归加入前） | 1429 passed / 1 skipped / 1 warning，1046.17秒，退出0；核实非超级用户/不可建库/建角色，集群已清理 |
| 新增回归 | 34项：22项HTTP/并发/原子性控制，12项源码/bundle前端；首轮相关112通过 |
| 控制反例 | 8项实际测试失败检出，非收集错误；原文件逐字节恢复后完成双后端 |
| 收尾定向组 | 提示文案细化后，控制/前端/文档181通过，47.15秒 |
| 文档 | 契约283文件/13所有者/0错误；178文件1947段讲解，125模块532依赖边61路由1715符号54文档；不是整库语义认证 |
| 静态/构建/审计 | Ruff、JS语法、pip check；npm audit 0；pip-audit --strict沿用PYSEC-2026-1325例外通过（2 ignored），无新增应用依赖或豁免 |
| 范围完整性 | 全部SQL（含0001–0015及full_init）字节未改；Skill源/分发副本相同；没有真实渠道、业务库或部署操作 |

反例：摘要漏控制序号导致同秒ABA、原key漏操作者绑定、重排绕过8次、接管仍可自动领取、任务提前commit导致审计失败不回滚、原key不再仅读、控制端点漏来源保护、前端未知请求换body/key。双后端后只细化已领取/尚未发出HTTP的边界提示及展示中文，补充同段精读说明；收尾定向组结果与最终精确SHA六项CI分别实际核对，不借用第十一批绿色。

历史跳过项不当作已执行，既有passlib/crypt弃用警告未掩盖；Node VM不是实际浏览器/Windows验收。首次跨单测试重复通知ID被正确拒绝，修正夹具独立ID；一次性UI脚本误重复执行后只恢复本轮两个UI文件再单次生成，并实际重建/回归，没有覆盖用户文件。

接管的界线是持久领取，而不是HTTP到达渠道：领取已提交的尝试，即使尚未发出HTTP，也可能在接管返回后继续GET；接管保证不再领取该暂停任务，并使旧结果不能覆盖当前任务，不是网络召回。系统结算授权/额外预算仍需另行设计，不将本批冒充自动财务闭环或真实上线签收。

## CI超时的实证与修复

首次发布a07f67b的运行35396917535：五项成功，PG被20分钟上限取消。通过check-run annotations核实明确超时消息；两种日志下载均EOF，未据此声称断言失败或通过。SQLite在CI用19分48秒，预算余量不足。仅SQLite/PG全量job改35分钟，其余四项20分钟不变；pytest仍完整运行且pipefail传播失败，无删测试/skip/弱化断言。另加一项工作流范围与预算回归，35项新增含34业务回归；最终CI重新按新SHA核对，不复用已取消运行。

CI预算修订后收尾组182通过（48.34秒）；另将预算临时退回20分钟，新增回归确实失败，随后逐字节恢复原工作流。这是预算门禁反例，不冒充第九项资金逻辑反例。
