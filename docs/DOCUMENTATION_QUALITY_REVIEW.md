# 文档内容与可读性复核

日期：2026-09-12。复核起点：第二批实现提交 `4e3c3c2`。

## 结论

原说明书存在实质质量问题，不能用“文件都登记了、链接能打开”证明其可用。本轮重写当前模块/架构/交接说明，补充关键函数契约并修复生成展示。原始新旧提案及合并审查原稿保持不变。

标准是维护者能回答：谁调用、输入输出是什么、是否查库/提交/请求外部服务、哪里会失败、哪些状态不能混同、改动后应运行哪些测试。不是按字数、每行代码配一句翻译或固定模板占位验收。

## 发现与处理

| 问题 | 具体证据 | 处理 |
| --- | --- | --- |
| 历史数字伪装当前规格 | 多份说明仍引用早期文件数、39 路由、旧测试结果 | 当前指南不固定手写统计；可复算数量来自构建，实际测试绑定提交 |
| 分层描述不真实 | “路由没有业务逻辑”“所有工作都在 tools”“各链无依赖” | 按实际 SQL 查询、事务与共享依赖重写，不把理想分层当既成事实 |
| 函数只翻译名字 | 缺返回值、提交边界、取消后任务占位、None 与 [] 区别 | 公共层/路由/工具采用函数契约表；解释 commit、失败和调用要求 |
| 下载规则过时 | 把 downloaded 写成权益已消耗、重复领取禁止 | 区分付款权益、发链接记录、传输完成与链接 bearer 性质 |
| 动态功能承诺错误 | 安装 Playwright 就能恢复；预检查即可保证浏览器网络安全 | 明确 _goto 停用；安装检测不等于功能可用，静态与动态分别说明 |
| 数据库重复运行误导 | “幂等”易被理解成无损升级；0008 重跑仍清码 | 初始化/增量迁移分开；强调 DROP、旧库基线、备份、停写及重新登录 |
| 模型能力说得过满 | 兼容 URL、Mermaid 前缀、RAG 引用被说成完整保证 | 说明协议/语法/事实各自边界；阈值仍需真实语料标定 |
| 源码说明与 README 冲突 | Word 仍写两步线程池、旧下载说明、Cookie 绝对安全措辞 | 同步修改对应 docstring/注释，符号页展示原文而不编造补白 |
| 源码字段提取但不展示 | 符号卡只有名称，没有参数/返回注解/说明 | 新增展开区、缺说明提示、测试符号可见性开关；HTML 转义测试 |
| 排版与文本损坏 | 中文替换字符；窄屏 sidebar 规则被后面的基础规则覆盖；340px 卡片撑开小屏 | 重写损坏正文，门禁拒绝乱码；末尾移动覆盖、长签名换行、键盘焦点与减少动画支持 |
| 空壳章节能蒙混过关 | 自动清单本身可填充“非空”章节 | 检查时剥离自动区，拒绝空/纯 TODO 等占位；空文件不再显示 L1–L0 |
| HTTP 契约与实现不符 | save_article 校验抛 ExtractError，但调用在 ingest 的 try 外 | 纳入既有错误映射；去掉 parse_page 对标题/作者/日期的提前截断，与保存层明确拒绝策略统一；真实解析/保存校验与数据库断言验证 422 |
| 干净 CI 与本地不同 | tee 生成 docs-build.txt / pytest-output.txt，被未知文本清单当新源码 | 在干净归档工作树重现；精确 Git/镜像排除两种临时日志，普通未知后缀源码仍受门禁 |

## 审查范围与保留边界

- 重写公共后端、路由、工具、数据库、测试、脚本、CI 的当前模块说明，以及根配置说明、架构指南和交接入口。
- 核对并补充前端关键函数/状态，检查模板与静态资源职责，更新根 README、部署、Windows 指南、文档站说明和文档策略。
- 对关键认证、付款/下载、消息、任务池、解析/入库和检索流程对照实际源码；简单 helper 按职责分组合述，不逐个复制函数体。
- TECH_DECISIONS、ROADMAP、原始报告中的旧日期记录是历史证据，不作为当前操作指南；新旧用户方案保持原文，只在比较文档中评议。
- Python AST 给出完整符号位置和声明；JS/HTML/SQL/CSS 目前仍是文件级自动覆盖，前端关键函数解释是人工核对，不声称跨语言解析器已经完成。

## 验证记录

第二批提交已成功推送。其首次远端运行 `34688811410`：Ruff、依赖审计、前端构建通过；文档和两种数据库测试失败。已在干净归档树重现 CI 日志参与源码清单的问题，本轮修正后重新验收，不拿首轮部分绿灯冒充完成。

- 全量 SQLite：**744 passed，6 skipped**；全量 PostgreSQL：**749 passed，1 skipped**。五条 PostgreSQL 专用测试只在 SQLite 跳过，剩余一条需真实模型标定。
- 两套全量均只有一条 Passlib crypt 弃用警告；没有用新增 skip 掩盖失败。
- 定向 HTTP/提取/文档测试 **73 passed**；包含签名/docstring 展示与转义、Node 过滤状态、乱码/占位、空文件、临时日志隔离，以及实际生成标题/代码块计数。
- 文档站重建：**30 文档页、237 源码页、47 路由**；实际本地链接/锚点/源码范围校验通过。Ruff 通过。
- 有控制的变异验证：把 save_article 移出错误映射 → **2 条回归失败**；让自动区参与人工章节检查 → **3 条失败、1 条乱码控制用例通过**。已按原字节恢复，恢复后 HTTP/文档 **57 passed**。
- 最终远端 CI 必须绑定最终提交查询，交付回复提供运行链接，不把上一次失败或局部通过写成全绿。

## 视觉验收的明确限制

检查了生成 HTML、CSS 层叠/响应式规则、长签名展示、可折叠结构、键盘焦点和 Node 过滤交互。尝试在独立临时工具目录安装 Playwright 浏览器，但 Chromium 下载因网络连接失败，未安装到应用虚拟环境，也未改变运行时依赖。
**因此没有真实浏览器截图或 Windows 桌面视觉验收**，不能把 CSS/DOM/Node 检查称为实际渲染验收。交付提供本地离线站，可由用户继续在真实设备检查；外部编辑器/模型/商户联调也不在本次结果中。

## 后续维护规则

先读源码并修改人工解释，再刷新指纹；正常检查不自动改说明。每个关键流程至少说明输入、输出/响应、权限、写入与失败边界。不要写没有证据的“一律”“完全”“任何环境”“零风险”。
人工审查无法被字数或 JSON 状态自动证明；门禁只阻挡可机械发现的遗漏、乱码、过期引用和空壳，不宣传成语义正确性证明。
