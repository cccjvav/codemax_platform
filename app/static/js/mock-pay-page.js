var e=document.getElementById(`no`).textContent.trim(),t=document.getElementById(`out`);document.getElementById(`pay`).addEventListener(`click`,async()=>{let n=await fetch(`/shop/mock-pay/confirm`,{method:`POST`,credentials:`same-origin`,headers:{"Content-Type":`application/json`},body:JSON.stringify({order_no:e})}),r=await n.json();t.textContent=n.status===200?`支付成功（模拟）\n状态：${r.status}\n模拟流水号：${r.transaction_id}`:n.status===401?`未登录：请先在工具页登录后再来。`:`失败 ${n.status}：${r.detail||JSON.stringify(r)}`});